# paste-fold

大段粘贴在输入框里折叠为一行摘要的显示方式。

现状核对（2026-08-23）：

- 编辑器缓冲是纯文本行（`Vec<String>`），粘贴原文全量插入、光标逐字符移动；
  **没有**摘要块，也没有「整块当单元」的光标语义。

设计核心问题：

- 左右方向键移过 `[Pasted ~N lines]` 摘要时，能否把整个块当作一个单元
  ——一次按键跨过整块、删除一次删掉整块？
- 可行方向：给编辑器引入「不可拆分的行内区间」（粘贴区以单行占位入缓冲，
  提交时展开回原文），或等价的原子块机制。
- 提交语义不变是 MUST：模型收到完整原文。
