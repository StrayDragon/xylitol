# turn-usage

每轮回复结束后，显示一行可展开的用量小结。

预期行为：

- 一轮回复结束，transcript 里追加一行折叠条目：
  `+ Turn: 4 steps · 1.2k new · 56.0k cached · $0.03`（步骤数 / 新增 tokens / 缓存命中 tokens / 花费）。
- 默认折叠；点击或用既有展开交互看逐步明细，不新增快捷键。
- 数据来自会话已有的用量记录；没有用量数据就不渲染这一行。
