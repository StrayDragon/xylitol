# toast-stack

右上角通知栈：只承载关键确认类通知，配专属图标。

预期行为：

- 专属图标 `◆` 起头（单格宽，与折叠箭头 `▸▾`、提示符 `❯` 区分）；
  成功完成类用 `✓`。
- **只显示关键通知**，例如复制成功、导出完成；过程性 / 低价值消息一律不进栈。
- 右上角纵向堆叠（最多约 3 条），逐条自动消失。
- 错误类仍走现有通知条行（status 上方恰好 1 行）；分工采纳时写入 固定区词表。
- 纯键盘可达：Tab 聚焦最新一条，回车执行动作。
