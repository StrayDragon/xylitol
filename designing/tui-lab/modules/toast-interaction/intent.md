# toast-interaction

通知条的两点交互增强。

预期行为：

- 鼠标悬停在通告上时，通告**不会自动消失**；移开后继续剩余的显示时间。
- 通告可以带一个操作按钮：行尾 `› retry` 这样的字样，点击（或按回车）执行对应动作，
  执行后通告立即消失。
- 同时有多条待显示时，合并为一条 + 计数（如 `+2 more`）；仍然只占一行。
- 位置、单行高度、出现时机都不变——这是对现有通知条的能力补充，不是新组件。
