# footer

恰好 1 行 dim。字段序 `cwd · model · {thinking}`，可续 `used C/~C/? tokens`（紧凑如 `42k`）与占用比。

- pending 时本行 model/thinking 仍是 **生效中**；下轮预告在 status 右侧，不进 footer。
- 默认不写快捷键提示；禁止队列徽章；Working / spinner **MUST NOT** 进本行。
