# loaded-resources

scrollback 上方 Codex 风边框卡片：`>_ xylitol` + directory。有 skills 时 skill-ref 色 skills 行并换行全量；有 MCP 时 success 色 mcp 行。

- 连接中 mcp 行可显示 `connecting i/n`；完成后收敛为 connected 摘要。
- 无 skills/MCP 仍保留卡片（title + directory）。失败可感，禁止逐步滚动提示刷墙。
- 禁止：木糖醇中文标签、ASCII logo、省略号截断资源名、列出 prompt templates、展示密钥。会话名/路径仍并进 footer。
