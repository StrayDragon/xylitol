# palette

语义色短表：on-surface / muted / accent / user / assistant / tool / error / warning / success / diff-* / surface / skill-ref。

- 禁止模块手写 hex。light 伴侣在 DESIGN `colors_light`，产品默认仍走 dark。
- YAML `token: muted` 映射同名色。
