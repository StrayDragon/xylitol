# editor

输入贴底，上下 `─` 边框。树 / Ask / `/model` 等选择器 **替换本槽**，Esc 还原；禁止 blit 到 transcript 顶。

- 忙碌 Enter=steer、Alt+Enter=follow-up，可见反馈在队列条，不在本框堆快捷键墙。
- idle / steer / follow-up 写入发送历史；空框 ↑↓ 召回。slash **不**进历史。
- Ctrl+G 走 TTY 真 `$EDITOR`（未配置则错误行，不默默 nano）。`!` bang 见 bash。
