# models

`/model` 同时选模型与 xylitol thinking 档。↑↓ 选模型；←→ 或槽内 Shift+Tab cycle 档。

- 宽：铺开全部档，当前高亮（如 `[high]`）。
- 窄：单档标签，不挤一行。
- 无思考：行上 `—`，禁止假装 `thinking off`。
- 选中行反转，正文不带 kind 色。
- 键入即 filter 行：`filter: {q}`。
