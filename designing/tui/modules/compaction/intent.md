# compaction

已完成压缩是 transcript 块：`[compaction] Compacted from {n} tokens`，折叠提示 `Alt+E to expand`。

- 进行中 `Compacting` 走 status 短词，**不**在本块。
- Alt+E 与 expandable 工具块同源：hide ↔ unhide。本稿 `collapsed` / `expanded`。
- 禁止 `compaction complete` 这类完成口号。
