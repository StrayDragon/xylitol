# ask

等待态簇底标题 `Asking questions`，**不**画三角。Ask 块必须可交互。禁止 `Planning next moves`。

- 对人：editor 槽问卷 + scrollback 人话摘要；JSON 只给模型。
- 选中 accent + 粗体（`→` / `[x]`），**MUST NOT** 默认整行 reverse。
- prompt 与选项之间空一行。窄宽说明只在焦点项下；宽屏可右栏说明。
- Ask ≠ Trust ChoicePrompt。结束后 scrollback 走人话摘要，不是 JSON tool 块。
