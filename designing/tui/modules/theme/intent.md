# theme

**产品 host 默认 dark**。本稿预览默认 light 只为审色，不是产品默认。

- `/theme` 无参开 dark/light 列表；有参 `dark`|`light`|`toggle` 直切。
- 禁止默认 theme auto / OSC11 / COLORFGBG。禁止把 demo Ctrl+P 当产品键。
- 色只经 DESIGN token，禁止组件散落 hex。一屏最多一处 accent（spinner 或焦点边框）。
