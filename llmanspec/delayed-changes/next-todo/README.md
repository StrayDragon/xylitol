# delayed-changes / next-todo

2026-08-10 曾在提案头写「已升格 / 排序 #N」，**目录从未迁入** `llmanspec/changes/`。公开预览与 SDD graph 都以本目录仍为 **park** 为准：不要当成待落地队列。

升格 = 把提案拷回 `llmanspec/changes/` 再走 propose → start。归档时删掉这里的副本（一 id 一处真值）。
