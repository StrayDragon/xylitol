# c27-add-fake-provider Tasks

- [ ] 基于 c25 的 Provider trait 实现 FakeProvider：文本响应 + 工具调用 + 多轮场景编排
- [ ] 定义 ScenarioStep 枚举 + Builder 模式配置
- [ ] 添加延迟模拟和错误注入能力
- [ ] 新增 `dev-fake-provider` feature flag
- [ ] 编写 FakeProvider 单元测试
- [ ] `just fmt && just lint && just test`
- [ ] `llman sdd validate c27-add-fake-provider --strict --no-interactive`
