# c100-improve-test-harness Tasks

- [ ] 创建 `with_test_timeout` helper（统一 test harness 超时）
- [ ] 为 async 测试包裹 timeout wrapper（防止 CI 挂起）
- [ ] MockToolContext 添加 `workspace_root` 字段
- [ ] `just fmt && just lint && just test`
- [ ] `llman sdd validate c100-improve-test-harness --strict --no-interactive`
