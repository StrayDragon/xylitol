# Tasks: provider-adapter-llmadapter-fake-thinking-level-ensure

测试 seam：复用既有 seam，不新扩 BDD step、不打网——主 crate 各模块内联单测（adapter / fake / manager / dispatch / driver remote）、集成测试 `tests/provider_http_stream_abort.rs`、BDD 套件（fake 驱动的 scenario 步骤：provider 集成 / domain compaction 等）、bridge 包内单测（thinking / fake / native adapters）。本 change 无行为合约变更（pa1 / r38 为结构不变量措辞随动），**既有测试断言零改动**是硬验收线；改断言才能过 = 实现错。

- [x] t1 specs：绑定分支上落地 specs——`infra-provider` 重写 `@req:pa1`（adapter-trait → 单层适配外壳不变量：AdapterXyModel 直接持有 bridge adapter、经 infra map 映射、MUST NOT 在 XyModel 外壳与 bridge adapter 之间保留 1:1 包装 trait 层）+ `test-fake-provider` 的 `@req:r38` 措辞随动（「实现 XyModel」→「经统一装配路径暴露为 XyModel」，ScenarioStep 契约原样）。`pa6` / `pa7` 不动。`llman sdd validate` 结构绿
- [x] t2 adapter 塌缩 [blocked-by: t1]：删 `LlmAdapter` / `MappedBridgeAdapter` / `AdapterRef`（`src/infra/provider/adapter/mod.rs`）；`AdapterXyModel`（`xy_model.rs`）直接持 bridge `AiBridgeAdapterRef`，generate/generate_stream 内联 `to_bridge_tools` / `to_xy_error` / `to_xy_stream` 调用（函数留在 `map.rs`）；`adapter/factory.rs` 直接构造 `AdapterXyModel`；引用随动：`src/infra/provider/mod.rs`、`tests/provider_http_stream_abort.rs`。验证：主 crate `cargo test`（infra/provider + BDD provider 套件）全绿
- [x] t3 fake 双轨收拢 [blocked-by: t2]：bridge `FakeProvider` 改实现 `AiBridgeLlmAdapter`（generate=非流式 / generate_stream=流式，options 忽略），删 `AiBridgeModel` trait 及其 re-export；主仓 `src/infra/provider/fake.rs` 新 type 退役 → re-export bridge `FakeProvider` + 经 `AdapterXyModel` 的构造助手；调用点随动：`src/infra/provider/mod.rs`、`factory.rs` BDD fake 路径、`src/agent/compaction/mod.rs`、`tests/bdd/steps_domain_compaction_extra.rs`、3 个 `packages/xylitol-ai-bridge/examples/lab_*.rs`、`tests/lab_responses_prompt_cache.rs`（如涉 AiBridgeModel）。验证：fake 单测 + bridge 测试 + BDD（domain compaction / provider 集成）全绿
- [x] t4 thinking 内置词表常量化（独立）：bridge `thinking.rs` 暴露 `AiBridgeBuiltinThinkingLevels`（off/minimal/low/medium/high/xhigh/max，`ALL` 表），`canonical_known_level` / `builtin_anthropic_budget` / `budget_for_level` 与 Default 改引之，附表自检测试；lib.rs 再导出；`protocol/model/thinking.rs` 以 `BuiltinThinkingLevels` 再导出（ThinkingBudgets 先例）。主仓随动核查：生产同义点均已 `THINKING_OFF`；剩余字面量为测试 fixture（声明式 opaque 档名，按设计保持）与 `xylitol-tui` 展示层（零依赖包，禁引 bridge）。验证：bridge thinking 单测 22 + protocol 132 全绿
- [x] t5 `ensure_downlink` 拆臂（独立）：共享 clone 字段收拢为 `DownlinkCtx` 结构；拆出 `downlink_loop`（连接循环）/ `handle_frame`（消息臂）/ `reconnect_pause`（ath41 退避暂停）/ `store_resources_snapshot`（与 `apply_resources_cache` 共用，消除臂内重复），ath41 / ath42 / ath44 / c2480 注释随代码搬迁。行为零变化。验证：`remote.rs` 内联测试 22 全绿
- [x] t6 全量门禁：`just fmt` + `just lint` + `just qa` 全绿（1920 tests + live-provider + doc-check；附带修复 c2610 遗留的 `session_ops.rs` 私有项文档死链，非本单合约面）；`llman sdd validate <id> --strict --no-interactive` 通过
