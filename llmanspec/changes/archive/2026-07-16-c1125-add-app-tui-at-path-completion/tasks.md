# Tasks — c1125-add-app-tui-at-path-completion

- [x] 1. Delta + design + tasks 齐；`llman sdd validate c1125-add-app-tui-at-path-completion --no-interactive`
- [x] 2. `UiRoot`：`at_path_base` + `install_completion_sources` 注册 `AtPathSource`
- [x] 3. harness：tempdir 下 `@` 弹出与 Tab 插入路径
- [x] 4. `just fmt` + lint + 相关 harness；`validate --strict`
