kind: llman.sdd.delta
ops[1]{op,req_id,title,statement,from,to,name}:
  add_requirement,sr10,"export-io-wiring","The server runtime composition root MUST construct an infra-provided StdExportIo and inject it as Arc<dyn runtime_protocol::export::ExportIo> when assembling the Agent hosted in AppState.",null,null,null
op_scenarios[1]{req_id,id,given,when,then}:
  sr10,"server-injects-export-io",the server runtime assembles the Agent,its constructor arguments are inspected,an Arc<dyn ExportIo> is passed
