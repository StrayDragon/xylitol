kind: llman.sdd.delta
ops[1]{op,req_id,title,statement,from,to,name}:
  add_requirement,sr-remote1,RemoteDriver 命令对齐,"RemoteDriver MUST 实现与 InProcessDriver 对等的文档化 Driver 命令面（模型/会话/队列/导出等），MUST NOT 在生产路径对已承诺命令返回未实现错误。",null,null,null
op_scenarios[1]{req_id,id,given,when,then}:
  sr-remote1,remote-commands,"","调用 RemoteDriver steer 与 queue_stats","成功并与 server 状态一致"
