kind: llman.sdd.delta
ops[1]{op,req_id,title,statement,from,to,name}:
  modify_requirement,ath12,"host-module-boundaries",产品 TUI MUST 将 HostSession 协调逻辑与可单测切片分离：pending 副作用标志 MUST 收拢为 PendingOps（或等价单一结构）；输入策略（busy/idle Esc、steer、bang 拒绝）与 session tree/models/resume mount 及 apply 切换 MUST 位于 host 子模块或独立文件；UiRoot 的 slash 目录与 slot 输入路由 MUST 可位于 layout 子文件；effects 的 slash 副作用 MUST 可按族切文件且仍经单一 drain_pending；bridge 的 UiModel 与 apply_xy_event MUST 可分文件；组件与 layout MUST NOT 直接调用 Driver；host/mod.rs、layout/root.rs、effects 入口、bridge/mod.rs 各 MUST 显著低于约 800 行（逼近约 1200 行仍视为硬味，MUST 优先拆分而非继续堆叠）。,null,null,null
op_scenarios[3]{req_id,id,given,when,then}:
  ath12,"god-files-under-budget",拆分后统计行数,审查 host/mod layout/root effects 入口 bridge/mod,各文件显著低于约 800 行且无第二套 drain 泵
  ath12,"input-policy-module",审查 host 目录,定位 busy Esc 与 idle Enter,位于 input_policy（或等价）子模块且 step 仍为唯一调度入口
  ath12,"effects-slash-split",审查 effects 目录,定位 PendingSlash 臂,按族分文件且 harness 仍调用同一 drain_pending
