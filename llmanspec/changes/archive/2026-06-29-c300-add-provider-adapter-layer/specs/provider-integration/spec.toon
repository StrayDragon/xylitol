kind: llman.sdd.delta
ops[1]{op,req_id,title,statement,from,to,name}:
  modify_requirement,pi0,"provider-registration","System SHALL support provider registration with configurable API keys base URLs headers and adapter api type for OpenAI-compatible and Anthropic providers.",null,null,null
op_scenarios[1]{req_id,id,given,when,then}:
  pi0,config-api,"a model config includes an api field",the factory builds the provider,"the correct adapter is injected into the provider"
