# c1585 Tasks

## 1. 默认 OneAtATime

- [x] 1.1 `QueueMode` / `SteeringMode` `#[default]` → OneAtATime
- [x] 1.2 单测：缺省 drain 两条只取 1；文档一句
- [x] 1.3 test: `cargo test -p xylitol --lib -- default_mode_is_one queue::`

## 2. 合约

- [x] 2.1 修订 `ar8` / `rc12` + feature
- [x] 2.2 attach + validate + finalize
