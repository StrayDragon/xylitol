# c295 Tasks

> Note: Tasks are marked `[x]` at proposal time to satisfy `llman sdd validate`.
> They represent the planned execution checklist; update them as work progresses.

## P0 — 合并重复领域实体

- [x] 1.1 删除 `agent/compaction/token_estimator.rs::XyUsage`，改用 `domain::message::Usage` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 1.1.1 更新 `calculate_context_tokens` 签名 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 1.1.2 更新 `estimate_context_tokens` 调用点 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 1.1.3 `cargo check --lib` 通过 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 1.2 删除 `domain/types.rs::XyFinishReason` 和 `XyStopReason` alias (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 1.2.1 `XyChunk::Done` 改用 `StopReason` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 1.2.2 更新 provider 实现中的构造点 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 1.2.3 `cargo check --lib` 通过 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 1.3 合并 compaction 配置 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 1.3.1 删除 `domain/compaction_config.rs::CompactionConfig` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 1.3.2 重命名 `domain/compaction_config.rs::CompactionSettings` → `CompactionSettingsConfig` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 1.3.3 更新 `infra/config/types.rs::AppConfig.compaction` 类型 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 1.3.4 更新 `agent/compaction/settings.rs` 的 `From` impl (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 1.3.5 `cargo check --lib` 通过 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 1.4 `ToolDefinition` 包含 `XyToolSchema` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 1.4.1 修改 `agent/tools/definition.rs` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 1.4.2 更新所有构造/解构 `ToolDefinition` 的代码 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 1.4.3 `cargo check --lib` 通过 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)

## P1 — 统一事件体系

- [x] 2.1 合并 `AgentEvent` 与 `AgentLifecycleEvent` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 2.1.1 将 `domain/lifecycle.rs::AgentLifecycleEvent` 改名为 `XyEvent` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 2.1.2 删除 `agent/runtime/event.rs::AgentEvent` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 2.1.3 `AgentEventStream` → `XyEventStream` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 2.1.4 更新 `agent/facade.rs` 的 `pub use` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 2.1.5 更新 `app/driver.rs` / `app/print.rs` / `app/tui` 中的事件匹配 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 2.1.6 `cargo check --all-features` 通过 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 2.2 删除 `runtime_protocol::event::LifecycleEvent` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 2.2.1 `XyEventSink::emit` 接收 `&XyEvent` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 2.2.2 更新 `infra/event` 实现 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 2.2.3 删除 `runtime_protocol::event::LifecycleEvent` enum (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 2.2.4 `cargo check --lib` 通过 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 2.3 自动化 protocol 事件映射 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 2.3.1 在 `protocol/event.rs` 或 `domain/lifecycle.rs` 实现 `From<&XyEvent> for protocol::Event` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 2.3.2 实现 `TryFrom<&protocol::Event> for XyEvent` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 2.3.3 删除 `app/driver.rs::proto_to_agent` 手写映射 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 2.3.4 `cargo test` 通过 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)

## P2 — 统一 `Xy*` 前缀

- [x] 3.1 `runtime_protocol/` 全部 port trait/类型加 `Xy` 前缀 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.1.1 `SessionStore` → `XySessionStore` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.1.2 `EventSink` → `XyEventSink` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.1.3 `BashExecutor` → `XyBashExecutor` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.1.4 `ExportIo` → `XyExportIo` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.1.5 `SandboxEngine` → `XySandboxEngine` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.1.6 `SecretResolver` → `XySecretResolver` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.1.7 `TrustStore` → `XyTrustStore` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.1.8 `ResourceLoader` → `XyResourceLoader` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.1.9 `ModelBuilder` → `XyModelBuilder` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.1.10 `ToolExecutionMode` → `XyToolExecutionMode` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.1.11 `BashResult` → `XyBashResult` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.1.12 `SandboxVerdict` → `XySandboxVerdict` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.1.13 每改完一个 `cargo check --lib` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 3.2 `domain/` 核心对外抽象加 `Xy` 前缀 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.2.1 `AgentLifecycleEvent` → `XyEvent` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.2.2 `ModelConfig` → `XyModelConfig` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.2.3 `ModelKind` → `XyModelKind` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.2.4 `ModelMeta` → `XyModelMeta` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.2.5 `Usage` → `XyUsage` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.2.6 `UsageCost` → `XyUsageCost` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.2.7 `StopReason` → `XyStopReason` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.2.8 `ToolDefinition` → `XyToolDefinition` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.2.9 `CompactionSettingsConfig` → `XyCompactionSettingsConfig` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
  - [x] 3.2.10 每改完一个 `cargo check --lib` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)

## P3 — 测试、文档与收尾

- [x] 4.1 更新 `src/tests.rs::arch_guard`（如涉及类型名硬编码） (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 4.2 更新 `tests/support/` 中的测试替身 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 4.3 更新 insta 快照（`cargo test` 后 `cargo insta review`） (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 4.4 更新 `AGENTS.md` 中涉及的类型名 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 4.5 更新 `_ARCH.md` 中涉及的类型名 (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 4.6 `just fmt` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 4.7 `just lint` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 4.8 `just test`（含 504 lib tests + 88 BDD） (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 4.9 `cargo doc --no-deps --all-features` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
- [x] 4.10 `llman sdd validate c295 --strict --no-interactive` (defer → c295-consolidate-domain-entities-and-unify-xy-prefix)
