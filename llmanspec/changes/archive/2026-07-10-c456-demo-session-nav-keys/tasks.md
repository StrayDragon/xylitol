# Tasks — c456-demo-session-nav-keys

- [x] 1. Delta specs：`package-tui-tree-selector` pts4–pts6 + scenarios
- [x] 2. 包：搜索 + ←→ 翻页 + `status_suffix`；单测
- [x] 3. `agent_demo`：五档 filter（Ctrl+D/T/U/L/A）+ 树开 Ctrl+O 循环；槽 chrome / 搜索转发
- [x] 4. harness：搜索、filter、←→；回写 `session-tree.md` / keybindings / vs-pi
- [x] 5. `cargo test -p xylitol-tui --lib tree_selector` + `--test agent_demo_test`；`llman sdd validate c456-demo-session-nav-keys --strict --no-interactive`
