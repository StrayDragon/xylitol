# c27-add-fake-provider Tasks

- [x] 基于 c25 的 Provider trait 实现 FakeProvider：文本响应 + 工具调用 + 多轮场景编排
- [x] 定义 ScenarioStep 枚举 + Builder 模式配置
- [x] 添加延迟模拟和错误注入能力
- [x] 新增 `dev-fake-provider` feature flag
- [x] 编写 FakeProvider 单元测试
- [x] `just fmt && just lint && just test`
- [ ] `llman sdd validate c27-add-fake-provider --strict --no-interactive`
