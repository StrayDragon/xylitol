# Tasks：c2510 探索簇近窗默认收起

- [ ] t1 判定与配置：activity_fold 纯函数簇判定（纯读/检索簇、计数、阈值 ≥3、`auto_collapse_explore` 短路）+ settings 加载（runtime-config rc29 姊妹场景的配置单测随动）；库内边界单测转绿
- [ ] t2 收纳与后缀：degrade 近窗条件地板（探索簇默认 L2、显式展开保护）+ 簇头类目计数后缀格式化（进行时 Exploring 词形、计数随工具开始更新）；transcript harness 可执行场景转绿（`explore-cluster-collapse-and-expand` / `explore-cluster-suffix-counting`，HostPumpBdd 同族新 steps+bindings）
- [ ] t3 designing 随动：activity-fold intent.md 词表（后缀条目）+ 4 固定态 + draft.yaml 取舍；chrome 词表 G 类条目；`just gen-designing-index` 与 designing lint 过闸
- [ ] t4 门禁收口：`just fmt` / `just lint` / `just test`（含既有 BDD + 新增场景 + designing lint）；`llman sdd validate --strict`
