kind: llman.sdd.delta
ops[1]{op,req_id,title,statement,from,to,name}:
  add_requirement,atb1,Bridge 单缝翻译事件,"产品 TUI MUST 经单一 bridge 缝将 XyEvent 译为 UI-only 模型；渲染层 MUST NOT 直接 match XyEvent。未知变体 MUST 降级（tracing）且 MUST NOT panic。",null,null,null
op_scenarios[1]{req_id,id,given,when,then}:
  atb1,no-xyevent-in-render,"","审查 tui 渲染模块","无 XyEvent 匹配；事件经 apply_xy_event"
