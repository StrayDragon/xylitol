kind: llman.sdd.delta
ops[1]{op,req_id,title,statement,from,to,name}:
  add_requirement,rc9,mcp_servers 配置字段,运行时配置 MUST 提供 mcp_servers（或等价）字段以声明 MCP 服务器；缺省或空表示未启用 MCP。,null,null,null
op_scenarios[1]{req_id,id,given,when,then}:
  rc9,field,"",读取默认配置,mcp 未启用且字段可序列化
