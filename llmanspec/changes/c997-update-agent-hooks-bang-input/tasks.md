# Tasks — c997-update-agent-hooks-bang-input

- [ ] 1. `llman sdd validate c997-update-agent-hooks-bang-input --no-interactive`
- [ ] 2. `execute_bash` 前 dispatch `user_bash`；Blocked 不执行
- [ ] 3. wiring：`执行 bash` allow + block 场景
- [ ] 4. 确认无第二套 TUI-only hook 点；`validate --strict`
