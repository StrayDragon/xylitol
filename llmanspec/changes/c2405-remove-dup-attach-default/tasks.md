# Tasks

- [x] t1: 删除 `app-tui/spec.toon` tui2 行与 `app-tui.feature` @req:tui2 场景
- [x] t2: 删除 tests/bdd 中 attach-default binding 与 tui2 steps；`DEFAULT_ATTACH_URL` 字面量断言迁入 attach.rs 单测
- [x] t3: 触碰重编译后跑受影响 BDD + attach 单测；`validate --strict --no-check` 过；全量 validate 收口
