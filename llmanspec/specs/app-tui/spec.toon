kind: llman.sdd.spec
name: "app-tui"
purpose: "跨切面不变量索引。产品细节见 app-tui-*；引擎/harness 见 package-tui-*；CLI 默认进 TUI 见 cli-entry。"
valid_scope[2]: "主 crate","workspace 测试"
requirements[6]{req_id,title,statement}:
  tui2,"attach-default","产品 TUI 默认 MUST attach 本机 Host（默认 http://127.0.0.1:18790，可覆盖）。未在听 MUST 非零退出并提示先起 Host。MUST NOT 默认同进程直握操作器，MUST NOT 静默改 embed。print 与库嵌入不受本条约束。"
  tui3,"three-surfaces-coexist",print（cli）、server（HTTP 监听器）与 tui（交互面）应用面 MUST 共存；不得以统一为名删除任一传输面。
  tui4,"reuse-contract","产品 TUI MUST 经协议契约、应用缝与 agent 公共入口导入，MUST NOT reach agent 子模块内部或 infra；MUST NOT 依赖已删除的独立 domain 顶栏。斜杠语义 MUST 复用 protocol::Command。"
  tui5,"no-skeleton-spray","产品 TUI 面下每个新增源文件 MUST 在同一变更内由真实入口驱动；MUST NOT 用 #[allow(dead_code)] 落地未驱动骨架。死码分诊规则由架构 AGENTS 与 l8ng-audit-dead-code skill 承载。"
  "tui-index","capability-index","产品 TUI 行为细节 MUST 由 app-tui-host / app-tui-bridge / app-tui-transcript / app-tui-chrome / app-tui-input / app-tui-commands 各自约束；引擎与五层 harness MUST 由 package-tui-*（含 package-tui-testing）约束，不得在本索引重复实现史细节。"
  "tui-cs","TUI 只承担面本地","产品 TUI MUST 只执行面本地能力（键盘、绘制、TTY、本机编辑器、剪贴板）。依赖工作区、模型、MCP 或会话生命周期的能力 MUST 由 host 角色执行。产品 TUI MUST 经四象限客户端连 Host；print 与库嵌入 MUST 仍允许同进程。MUST NOT 把剪贴板等面本地能力交给远程 host 写入本机盘。"
scenarios[1]{req_id,id,given,when,then,feature}:
  "tui-cs","tui-cs-unit","产品 TUI 面本地归属 约束由架构约定与 attach 默认路径覆盖（非新 BDD harness）",审查实现与对应测试,"行为符合本 requirement 陈述；产品 TUI 默认 attach",false
