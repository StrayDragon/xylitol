kind: llman.sdd.spec
name: "workspace-structure"
purpose: "工作区结构 — 项目布局、模块组织与目录约定。"
valid_scope[2]: Cargo.toml,src/
requirements[5]{req_id,title,statement}:
  r1,"app-layer","System MUST 将用户面组织在单一 src/app/ 层：自包含入口 cli/、rpc.rs、server/、tui/、gui.rs 位于 app/ 顶层，跨面 seam（composition、driver）位于 src/app/core/。print 模式 MUST 位于 src/app/cli/ 作为 CLI 子模式。内置模块（tools、hooks、security）无条件编译；feature 门控 app 模式（server、tui、gui）使用 #[cfg(feature = \"...\")] 守卫，feature 名带 app 前缀（server、tui、gui）。"
  r2,"feature-flags","System MUST 为每个可选能力定义带域前缀的 feature flag，使非默认 feature 零开销编译剔除。内置能力（tools、hooks、security、repeat-detection、print-mode）无 feature flag，运行时由 config.yaml 控制。ACP 模式使用 feature flag infra-acp。"
  r3,"no-circular-layer-dependency","infra 层 MUST NOT 导入 agent 层；依赖方向 MUST 仅为 app -> agent -> infra，app 为唯一允许同时导入 agent 与 infra 的组合根。"
  r4,"unified-bootstrap","App 模式（CLI、RPC、Server、TUI）MUST 共享单一组合函数（app::core::composition::build_agent）构造 Agent，消除重复初始化逻辑。"
  r5,"minimal-default-features","默认 feature 集 MUST 仅含基本 CLI 所需（cli、rpc）；扩展 app 模式（server、tui、gui）MUST 为 opt-in 或归入 full alias。"
scenarios[5]{req_id,id,given,when,then}:
  r1,"app-layer-contains-entries","",变更后检查源码树,"src/app 含 cli/、rpc.rs、server/、tui/、gui.rs 与 core/ 子层（composition、driver）；print 位于 cli/ 下"
  r2,happy,"","cargo check --all-features 运行","所有 feature 门控模块无错误编译"
  r3,"app-composes-agent-and-infra","",新 app 组合根接线 Agent,"其为唯一同时 import crate::agent 与 crate::infra 的 app 模块"
  r4,"modes-use-build-agent","","CLI、RPC、Server 或 TUI 需要 Agent","它们调用 app::core::composition::build_agent 而非重复接线"
  r5,"server-feature-gated","","cargo check --no-default-features 运行",server 与 tui 代码未编译且 axum/ratatui 依赖未拉取
