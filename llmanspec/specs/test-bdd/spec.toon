kind: llman.sdd.spec
name: "test-bdd"
purpose: "BDD 测试框架：rstest-bdd — feature 文件、步骤定义与场景验证。"
valid_scope[2]: tests/bdd.rs,tests/features/
requirements[3]{req_id,title,statement}:
  r1,"bdd-all-pass","全部 77 个 BDD 场景 MUST 通过（当前 41/77，36 failed），使用 rstest-bdd runner。"
  bt1,"server-integration-scenarios","BDD 测试套件 MUST 含 server.feature（server 启动、health check、prompt 提交、event streaming、shutdown）与 approval.feature（经 reverse RPC 的工具审批往返）的步骤定义。"
  bt2,"rstest-bdd-current-and-app-tui","仓库 MUST 使用 crates.io 当前 rstest-bdd / rstest-bdd-macros 兼容最新 beta（本变更目标 0.6.0-beta3 或更新的同系列）；MUST 在 tests/features 增加产品 TUI 相关 .feature，并在 BDD runner（tests/bdd.rs 或其子模块）提供对应 step；既有核心 BDD 场景 MUST 在升级后仍全部通过。"
scenarios[3]{req_id,id,given,when,then}:
  r1,"all-pass",feature 文件中存在全部 77 个场景,"cargo test --test bdd -- --test-threads=1","77 个全通过，0 失败"
  bt1,"server-feature-steps",server.feature 场景有 Gherkin 步骤,BDD runner 解析它们,所有 server.feature 与 approval.feature 场景运行并通过
  bt2,"upgrade-and-core-green","Cargo.toml 已升至目标 rstest-bdd 版本","cargo test --test bdd -- --test-threads=1","既有核心 feature 与新增 app-tui feature 均通过"
