kind: llman.sdd.spec
name: "test-bdd"
purpose: "BDD 测试框架：rstest-bdd — feature 文件、步骤定义与场景验证。"
valid_scope[2]: tests/bdd.rs,tests/features/
requirements[5]{req_id,title,statement}:
  r1,BDD 全量通过,"BDD 场景集 MUST 保持全量通过（cargo test --test bdd 与 cargo test --lib bdd_scenarios）；未实现的孤儿 feature（如 event-bus）MUST 移除而非保留为债务，其行为由对应模块单测覆盖。"
  bt1,"server-integration-scenarios",BDD 测试套件 MUST 含 server.feature（server 启动、health check、prompt 提交、event streaming、shutdown）与 approval.feature（经 reverse RPC 的工具审批往返）的步骤定义。
  bt2,"rstest-bdd-current-and-app-tui","仓库 MUST 使用 crates.io 当前 rstest-bdd / rstest-bdd-macros 兼容最新 beta（本变更目标 0.6.0-beta3 或更新的同系列）；MUST 在 tests/features 增加产品 TUI 相关 .feature，并在 BDD runner（tests/bdd.rs 或其子模块）提供对应 step；既有核心 BDD 场景 MUST 在升级后仍全部通过。"
  tb3,solidify 链路 BDD,"BDD 场景 MUST 支持从 spec.toon scenarios 经 solidify 生成 llmanspec/specs/<capability>/<capability>.feature，并由 bdd.rs 的 #[scenario(path=..., name=<scenario.id>)] 消费；场景标题 MUST 等于 spec.toon 的 scenario.id 字段；该链路 MUST 与现有 tests/features/ 手写链路并存，互不干扰。"
  tb4,配置 BDD 分层,配置加载行为（三层合并、模型/provider 解析、ConfigValue 插值、默认值）MUST 由 src/infra/config 与 src/infra/settings 的单元测试覆盖，MUST NOT 保留无 step 实现的孤儿 feature 作为 BDD 债务。
scenarios[5]{req_id,id,given,when,then,feature}:
  r1,"no-orphan-features","tests/features 下所有 feature 均有对应 #[scenario] 绑定","cargo test --test bdd -- --test-threads=1",全量通过且无孤儿 feature 被跳过,false
  bt1,"server-feature-steps",server.feature 场景有 Gherkin 步骤,BDD runner 解析它们,所有 server.feature 与 approval.feature 场景运行并通过,true
  bt2,"upgrade-and-core-green","Cargo.toml 已升至目标 rstest-bdd 版本","cargo test --test bdd -- --test-threads=1","既有核心 feature 与新增 app-tui feature 均通过",true
  tb3,"solidify-pilot-passes","agent-runtime spec.toon 含 scenario react-terminates 与 stream-is-xyevent","cargo test --test bdd -- --test-threads=1",两个 solidify 风格场景通过且现有场景无回归,false
  tb4,"config-covered-by-unittests",src/infra/config 与 src/infra/settings 含覆盖三层合并/解析/插值/默认值的单测,"cargo test --lib infra::config infra::settings",配置加载行为由单测保障且无孤儿 feature 残留,false
