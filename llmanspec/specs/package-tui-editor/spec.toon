kind: llman.sdd.spec
name: "package-tui-editor"
purpose: "xylitol-tui Editor：视觉行图、粘滞列、PasteBurst、历史导航与边框色 API。"
valid_scope[2]: src/,tests/
requirements[9]{req_id,title,statement}:
  ed01,"visual-line-map","Editor MUST 经 build_visual_line_map(width) 从逻辑行构建 visual-line map，将每个 wrapped visual segment 映射到 logical line index、start column 与 byte length。map MUST 考虑宽于 width 的 word-wrap，空行 MUST 占一行 visual line。map 是跨 wrapped 行垂直光标移动的基础。"
  ed02,"sticky-column-vertical-move","Editor MUST 经 compute_vertical_move_column(current_col, src_max, tgt_max) 实现粘滞列垂直光标移动，遵循 P/S/T/U 决策表：（P=has preferred，S=cursor in middle，T=target shorter than current，U=target shorter than preferred）。editor MUST 存储 preferred_visual_col 与 snapped_from_cursor_col。set_cursor_col MUST 清除两者。move_cursor(dl, dc) 在 dl != 0 时 MUST 委托 move_to_visual_line。"
  ed03,"page-scroll","Editor MUST 用 build_visual_line_map 宽度相关布局实现 pageScroll(direction)，page size 为 max(5, terminal_rows * 30 / 100)。光标 MUST 经 move_to_visual_line 按 page 大小移动 visual lines，而非硬编码 logical-line 偏移。"
  ed04,"paste-burst-integration","Editor MUST 集成 PasteBurst（c415）。每次 plain character 插入时 MUST 调用 on_plain_char(now)。Enter（submit 或 newline）时 MUST 检查 should_insert_newline_instead_of_submit(now)；若为 true MUST 插入 newline 而非 submit。非 printable key 或显式 paste 时 MUST 调用 reset()。now Instant MUST 作为参数传入（非 wall-clock），遵循 c415 injectable-clock 模式。"
  ed05,"history-navigation","Editor MUST 提供 navigate_history(direction: isize)，使用 setTextInternal 与 cursorPlacement（上箭头 'start'，下箭头 'end'）。exitHistoryBrowsing() MUST 在每个应退出 history 模式的编辑动作（insert、delete、yank、submit）入口调用。从 draft 状态进入 history 时，当前 editor 状态 MUST 保存为 history_draft。"
  ed06,"highlight-via-callback","Markdown 代码块高亮 MUST 经 MarkdownTheme.highlight_code 回调注入；xylitol-tui 默认依赖 MUST NOT 包含 syntect；真实高亮实现 MUST 位于 optional feature（如 highlight）或应用面适配层。"
  ed07,"highlight-safety-limits",注入的 syntect（或等价）高亮 MUST 对过大输入施加字节与行数上限；超限时 MUST 回退为未高亮纯文本行，MUST NOT 阻塞事件循环。
  ed08,"set-border-color",Editor MUST 提供 set_border_color（或等价 API）以替换边框 ANSI 包装闭包，供 host 在 bash 模式等场景切换操作区边框色；MUST NOT 要求重建整个 Editor 实例。
  ed09,"paste-collapse-expand","Editor MUST 在 bracketed paste 内容超过 10 行或超过 1000 字符时，将缓冲区内文本替换为 [paste #N +L lines] 或 [paste #N C chars] 占位，并在内部 pastes 表保留原文；get_text MUST 返回含占位符的显示文本；get_expanded_text MUST 将每个有效 id 的完整 marker（含 +lines/chars 后缀）替换为原文，MUST NOT 残留 +N lines] 或 N chars] 碎片。短粘贴 MUST 直接插入原文。"
scenarios[14]{req_id,id,given,when,then}:
  ed01,"build-vl-map-wraps-long-lines",30 列 editor 中有一行 100 字符,build_visual_line_map(30) 被调用,map 含 4 个 visual line 条目，logical_line、start_col、len 正确
  ed02,"sticky-col-up-arrow",光标在第 2 行第 15 列，该行跨 3 个 visual lines,按下 cursor up,光标移到上一 logical line 对应 visual line 的第 15 列（或较短行尾）
  ed02,"sticky-col-cleared-on-horizontal",从第 15 列 cursor up 到第 8 列（行更短）设置 preferred_visual_col 为 15,word_left 被按下,preferred_visual_col 设为 None
  ed03,"page-scroll-moves-by-pagesize",terminal rows=40，page size=12,page_down 被按下,光标经 move_to_visual_line 下移 12 个 visual lines，而非 5 个 logical lines
  ed04,"paste-burst-enter-suppressed",9 个 fast chars 各 1ms 内到达，触发 paste burst，再 Enter,handle_input 收到 Enter,PasteBurst 对 should_insert_newline_instead_of_submit 返回 true，插入 newline 而非 submit
  ed04,"paste-burst-reset-on-nonprintable",burst 进行中,"按下 CursorLeft（non-printable）",paste_burst.reset() 被调用，清除 burst 状态
  ed05,"history-draft-restored","用户 up-arrow 浏览 2 条 history","down-arrow 越过最后一条回到 history_index=-1",保存的 history_draft 以原 editor 内容恢复
  ed06,"demo-injects-syntect",feature highlight 启用,agent_demo 渲染含 rust fence 的助手消息,输出行含 ANSI 颜色序列
  ed06,"default-no-syntect-dep","",cargo metadata 默认 features,"xylitol-tui 默认依赖树不含 syntect"
  ed07,"oversize-fallback",代码超过字节上限,调用 highlight_code,返回未高亮纯文本行且不 panic
  ed08,"border-swap",Editor 已构造,调用 set_border_color 后 render,边框行经新闭包着色
  ed09,"long-paste-collapses",Editor 收到超过 10 行的 bracketed paste,粘贴完成,"get_text 匹配 [paste #N +L lines] 且 pastes 表含原文"
  ed09,"expanded-equals-original",长粘贴已折叠为 marker,调用 get_expanded_text,"返回值等于粘贴原文且不含 [paste #"
  ed09,"short-paste-inline",粘贴不超过 10 行且不超过 1000 字符,粘贴完成,"get_text 含原文且无 [paste # 占位"
