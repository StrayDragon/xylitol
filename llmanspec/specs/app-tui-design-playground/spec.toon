kind: llman.sdd.spec
name: "app-tui-design-playground"
purpose: "DESIGN playground 静态设计图：生成物 SSOT、单一视觉真源指针、一致性 lint 入闸。"
valid_scope[1]: "产品 design playground 与视觉 SSOT（生成物与 lint 由面 AGENTS 承继）"
requirements[3]{req_id,title,statement}:
  adp1,"token-sync-ssot","playground token 生成物 MUST 由单一生成脚本从视觉 SSOT 生成；手改生成物 MUST NOT 作为长期真值。"
  adp2,"playground-static-consistency","playground 静图与视觉 SSOT 的静态一致性（槽示意、fixture、模板与文档指针）MUST 由 design 文档与 playground lint 承继维护；改静图须跑 lint 的指引 MUST 由面 AGENTS 写明。"
  adp6,"playground-lint-gate","仓库 MUST 提供 playground lint 脚本（或等价），并由 just qa 经 check-scripts 执行；失败时 MUST 非零退出。脚本 MUST 覆盖至少：禁止实现分层标签/置灰、选中 .rev 对比度护栏（CSS inherit 与选中行无 kind 前景穿透）、必有 panel-models 与 panel-tree-power 槽、Models/Tree 使用 term-slot、fixture 与模板源按 must_contain / must_not_contain / assert_html 断言。"
scenarios[3]{req_id,id,given,when,then,feature}:
  adp1,adp1-unit,token 生成物,运行 token 同步脚本,生成或刷新生成物且无报错,false
  adp2,adp2-unit,静图与 SSOT 一致性,审查 design 文档与 lint 覆盖,静态一致性由 lint 与 AGENTS 指引承继,false
  adp6,adp6-unit,playground lint 已接线,运行 playground lint --check,退出码 0 或对故意违规样本非 0,false
