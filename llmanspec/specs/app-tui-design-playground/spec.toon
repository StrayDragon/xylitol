kind: llman.sdd.spec
name: "app-tui-design-playground"
purpose: "交互设计稿：token 从视觉 SSOT 生成；模块固定态可闸；无快捷键墙；浏览器稿不是运行时真值。"
valid_scope[2]: "designing 应用与生成物","产品 TUI 视觉 SSOT（lint 与约定由面 AGENTS 承继）"
requirements[8]{req_id,title,statement}:
  adp1,"token-sync-ssot",产品 TUI 语义色 MUST 以视觉 SSOT frontmatter 为唯一色板数据；designing 生成的 tokens MUST 由单一同步脚本写出；手改生成物 MUST NOT 作为长期真值。
  adp2,"markdown-slot-align",Markdown 固定态示意 MUST 无井号标题前缀、链接为 text (url)、粗体斜体无可见星号包裹。
  adp3,"agent-default-ignore",面 AGENTS MUST 写明 Agent 改表面先读产品代码、默认读短模块 intent/states、默认忽略 designing 应用壳；仅在人类点名路径时才读应用壳源码。
  adp4,"session-tree-kind-ssot",会话树固定态 MUST 用 DESIGN token 色区分 kind 前缀；选中反转行 MUST 继承前景，MUST NOT 再把 role 字符串预烘焙成唯一表现。
  adp5,"session-tree-travel-ssot-live",会话树是 Driver MessageHistory 活树；Enter 经 travel（或等价）预填 user 输入。MUST NOT 再暗示产品仍为假树 stub。
  adp9,"static-slots","交互设计稿 MUST 提供 models（wide / narrow / no-thinking）、session-tree filter 与 pending next-turn 固定态；MUST NOT 在静图中表达实现分层（包标签）或顶栏快捷键墙；MUST NOT 设立独立快捷键百科模块。"
  adp6,"designing-lint-gate","仓库 MUST 提供 designing lint 脚本（或等价），并由 just qa 经 check-scripts 执行；失败时 MUST 非零退出。脚本 MUST 覆盖：模块 intent/draft/states 存在、states 的 must_contain / must_not_contain、禁止实现分层标签、选中反转 inherit、无独立 keybindings 模块。"
  adp8,"designing-lint-docs","面 AGENTS MUST 写明改设计稿须跑 designing lint 与 check-tui-tokens；MUST NOT 再暗示仅靠人眼发现色板或固定态漂移。"
scenarios[13]{req_id,id,given,when,then,feature}:
  adp1,sync-runs,,运行 token 同步脚本,生成或刷新 token 生成物且无报错,false
  adp2,md-slot,,打开 Markdown 固定态,示意中无井号标题前缀且链接呈 text (url) 形态,false
  adp2,no-star-wrapping,,查看粗体斜体示意,可见文案无 ** 或成对 * 包裹,false
  adp3,agents-pointer,,阅读面 AGENTS 设计稿约定,写明 Agent 先读产品代码、默认读短模块、默认忽略 designing 应用壳,false
  adp4,tree-slot-kind-colors,打开会话树固定态,查看树行,可见分色 kind 前缀与选中反转 inherit,false
  adp5,live-tree-note,,阅读会话树设计备注,写明活树与 travel（或等价）/ user 预填,false
  adp9,slots-present,,打开 designing,存在 models、session-tree、pending 模块且无包标签、无顶栏快捷键墙、无独立 keybindings 模块,false
  adp9,models-shape,,查看 models 打开态,替换 editor 槽示意且选中行为整行 reverse,false
  adp9,models-wide-levels,,查看 models.wide 固定态,焦点行铺开多档 xylitol level 且当前档有方括号标记,false
  adp9,pending-trail,,查看 pending next-turn 固定态,status 含 Next turn 文案且贴在 busy 行右侧,false
  adp6,qa-runs-lint,,运行 designing lint --check,退出码 0 或对故意违规样本非 0,false
  adp6,rev-inherit,设计稿 CSS 含选中反转,检查选中对比度规则,要求 color inherit 且选中行无 kind 前景穿透,false
  adp8,docs-pointer,,阅读面 AGENTS 设计稿约定,含 lint / qa 指针,false
