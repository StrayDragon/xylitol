kind: llman.sdd.spec
name: "runtime-model-registry"
purpose: "模型注册表 — provider 注册、鉴权检查、模型发现与解析。Pre-1.0.0 仅支持 OpenAI 兼容与 Anthropic provider。"
valid_scope[2]: "agent 层 model","protocol 层"
requirements[16]{req_id,title,statement}:
  m1,ModelRegistry provider,"ModelRegistry MUST 支持 register_provider(name, config) 与 has_configured_auth(model) / has_resolved_auth(model) 检查 API key 可用性。"
  m2,模型发现,ModelRegistry MUST 支持 get_available()，返回已注册 provider 的全部配置模型，按 provider 优先级排序。
  m3,模型默认值,"ModelRegistry MUST 仅为 openai 与 anthropic provider 维护默认模型 ID。Pre-1.0.0 其它 provider MUST NOT 有默认模型条目。默认模型 ID MUST 为有效、当前可用的模型标识（如 gpt-4o、claude-sonnet-4-20250514），不得为占位或不存在的模型名。该默认 ID 仅作显式解析/回退辅助，MUST NOT 单独构成「用户已配置默认模型」的产品选中态；MUST NOT 因仅存在 provider 环境变量而把该默认 ID 注册进可调用模型表（见 m12/m17）。"
  m4,模型解析器,"System MUST 提供 resolve_model(pattern, available)，支持 provider/modelId 规范形式、裸 id 匹配、别名优先于带日期版本、以及 thinking-level:model pattern 解析。"
  m5,作用域模型,"System MUST 支持 scoped_models（来自 CLI --models 标志），用于模型循环，将可用模型限制为用户指定子集并可含每模型 thinking level。"
  m6,模型回退,目标模型 ID 不在 provider 可用列表时，ModelResolver MUST 构建回退 Model，保留用户意图并回退到同 provider 基础模型。
  m7,鉴权指引,未配置模型 API key 时，ModelRegistry MUST 返回面向用户的错误消息，指引正确环境变量名。
  m8,BDD model,模型注册表与解析的 BDD 测试 MUST 全部通过。
  m9,"thinking-levels-on-meta","ModelRegistry / resolve_model_meta（或等价）MUST 将配置中的 thinking_levels 字符串列表填入模型 meta 支持集并保留配置顺序；thinking 为 false 或未声明可调列表时支持集 MUST 仅为 off（不可调）。MUST NOT 在未声明列表时静默展开历史 STANDARD（off…high）或其它全球超集；MUST NOT 要求档名属于封闭枚举超集；MUST NOT 依赖 xylitol-tui ThinkingBorderLevel。"
  m10,"thinking-level-validate-clamp","set_thinking_level（或 Driver 等价）在目标 level 不在当前模型支持集时 MUST 拒绝且 MUST NOT 更改当前值。匹配 MUST 为精确字符串相等（与配置声明字面量一致）；MUST NOT 因仅大小写或首尾空白差异而接受（例如声明 high 时 MUST 拒绝 HIGH 与「 high 」）。换模、精确 /model <id> 或路径要求默认档时：可调模型 MUST 采用支持集配置顺序的末项；仅精确 off 或不可调 MUST 为 off。MUST NOT 因 Settings.default_thinking_level 覆盖换模末项策略。会话 load/resume 还原分支末次 thinkingLevelChange 字符串时 MUST 原样恢复，MUST NOT 因集外而改写 JSONL 或自动追加 clamp 条目；配置漂移时 MAY 短暂 sticky 集外直至用户显式改档。产品面 MUST 展示当前模型声明的档名列表；provider wire 差异经 thinking_level_map 与 api×compat 在请求边界生效。"
  m11,"thinking-level-on-generate","Agent 在调用 generate_stream（或等价）时 MUST 传入当前 thinking 档名字符串与模型 thinking_level_map（及可用的 thinking_budgets）；resolve 后 MUST 影响下一轮 provider 请求体；MUST NOT 仅更新会话/UI 状态而不影响请求组装。"
  m15,"thinking-level-exact-opaque","运行时对 thinking 档名（支持集成员、set/cycle、Settings 默认是否 ∈ 支持集、可调判定中的关档字面量）MUST 使用精确 opaque 字符串；关档约定字面量 MUST 为精确 off。MUST NOT 在匹配或可调判定中对档名做 ASCII 大小写折叠或 trim 归一。"
  m12,"no-silent-env-select","bootstrap MUST NOT 在缺少用户显式模型别名配置时，仅因存在 provider API key 环境变量而向 ModelRegistry 注册 default_model_id_for_provider（或等价）默认模型；亦 MUST NOT 自动将该默认 ID 设为当前选中模型。无显式模型配置时 registry MUST 为空且经 bootstrap 的表面 MUST 硬失败（见 ce2）；未选中时应用面按 cli-entry ce18 展示。"
  m13,"honor-model-entry-api","bootstrap / resolve_model_meta（或等价装配）注册 XyModelMeta 时 MUST 保留 ModelsConfig ModelEntry 的可选 api 字段写入 config.api 与 meta.api：省略 api 时 MUST 走 AdapterKind::default_for（或等价）。显式可识别 api（含 openai-completions）MUST 参与 adapter 选择；未识别字符串 MUST 静默等同省略，MUST NOT 为此单独报错。MUST NOT 在注册路径把已配置的 api 字段写成 None。由单测覆盖，MUST NOT 为静态接线单独扩 BDD step。"
  m14,"manifest-api-kind-default","JSON model manifest 省略 api 时 MUST 按 provider 选用与 AdapterKind::default_for 一致的协议族字符串（OpenAI→openai-responses，Anthropic→anthropic-messages）；显式 openai-completions MUST 可选且参与装配。由单测覆盖，MUST NOT 为静态缺省单独扩 BDD step。"
  m17,"explicit-model-entry-auth","AppConfig ModelsConfig 中每个显式模型别名条目 MUST 在 bootstrap 注册进 ModelRegistry（供 --list-models 与选模），MUST NOT 因 API key 缺失或未解析而跳过注册。条目 api_key 省略或空串时注册 key MUST 为空；MUST NOT 回落 provider kind 级环境变量（OPENAI_API_KEY / OPENAI_KEY / ANTHROPIC_API_KEY / ANTHROPIC_KEY）。真发请求时缺 key MUST 失败并走鉴权引导（m7/ux4）。由单测覆盖，MUST NOT 为静态边界单独扩 BDD step。"
scenarios[28]{req_id,id,given,when,then,feature}:
  m17,"yaml-entry-registers-without-kind-env-unit","YAML 含 openai 别名且省略 api_key、环境无 OPENAI_API_KEY","bootstrap","别名在 registry；key 为空；未注入 gpt-4o",false
  m12,"no-env-invented-models-unit","无 YAML 模型别名且仅有 OPENAI_API_KEY","bootstrap","registry 为空且硬失败；MUST NOT 注册默认模型",false
  m15,"set-rejects-case-variant-unit","支持集含精确 high","set_thinking_level 为 HIGH","拒绝且当前值不变",false
  m13,"yaml-explicit-completions-api","YAML 模型条目 api 为 openai-completions","bootstrap 后 resolve_adapter_kind","选用 OpenAI Completions",false
  m13,"yaml-omitted-api-uses-kind-default","YAML OpenAI 模型条目省略 api","bootstrap 注册后 resolve_adapter_kind","选用 OpenAI Responses 默认",false
  m14,"manifest-openai-omitted-api-responses","JSON manifest OpenAI 模型省略 api","load_models_from_manifest","meta.api 为 openai-responses",false
  m14,"manifest-anthropic-omitted-api-messages","JSON manifest Anthropic 模型省略 api","load_models_from_manifest","meta.api 为 anthropic-messages",false
  m8,"bdd-model-pass",model BDD 套件,执行,全通过（由 CI 保证）,false
  m1,register-provider,以名称 openai 与 api key 注册 provider,对 openai 模型调用 has_configured_auth,返回 true,false
  m1,no-auth,anthropic 未配置 api key,对 claude 模型调用 has_configured_auth,返回 false,false
  m1,resolved-auth,api key 为已设置的环境变量引用 $MY_KEY,对 provider 调用 has_resolved_auth,返回 true,false
  m1,unresolved-auth,api key 为未设置的环境变量引用 $MY_KEY,对 provider 调用 has_resolved_auth,返回 false,false
  m2,available-models,两个 provider 各有 2 个模型,调用 get_available,按优先级返回 4 个模型,false
  m3,real-values,,对支持的 provider 调用 default_model_id_for_provider,返回真实可用模型标识而非 gpt-5.4 等占位符,false
  m12,no-silent-env-select,仅设置 OPENAI_API_KEY 且无配置模型且未传 --model,bootstrap 尝试装配,MUST 硬失败且 ModelRegistry MUST NOT 含 gpt-4o 或其它仅由 env 注入的默认模型；当前选中模型 MUST NOT 被自动设为 gpt-4o,false
  m4,exact-match,可用模型含 openai/gpt-4o,调用 resolve_model('openai/gpt-4o'),返回匹配模型,false
  m5,scoped-cycle,scoped_models 有 3 项,调用 cycle_model,仅在这 3 个模型间循环,false
  m6,fallback-model,用户在 openai provider 请求 nonexistent-model,解析模型,回退使用 openai 基础模型且以请求 id 为名称,false
  m7,auth-guidance-message,anthropic 模型未配置 API key,调用 auth_guidance_message,消息含 ANTHROPIC_API_KEY,false
  m9,default-off-when-unset,模型 thinking true 且无 thinking_levels,resolve_model_meta,thinking_levels 仅为 off,false
  m9,explicit-vendor-list,配置 thinking_levels 为 off 与 high 与 max,resolve_model_meta,列表为 off 与 high 与 max 且顺序一致,false
  m9,freeform-level-name,配置 thinking_levels 含厂商字面量 xhigh,resolve_model_meta,支持集含 xhigh,false
  m10,default-last-on-select,目标模型支持 off 与 high 与 max,select_model 到该模型,thinking level 为 max 而非 Settings 低档默认,false
  m10,no-thinking-is-off,目标模型 thinking 不可调或支持集仅 off,select_model 到该模型,thinking level 为 off,false
  m10,resume-restores-exact-level,会话分支末次 thinkingLevelChange 为 high 且当前模型支持集含 high,resume 或装载会话上下文,当前 thinking level 为 high 且未因装载追加新的 thinkingLevelChange,false
  m10,resume-sticky-out-of-set,会话末次 thinkingLevelChange 为 high 且当前模型支持集仅为 off 与 max,resume 装载,内存 thinking level 仍为 high 且会话文件未被改写为 max 或 off,false
  m11,level-reaches-options,当前 thinking level 为 high,发起一轮 generate_stream,调用携带 thinking_level 字符串 high（或等价 options）,false
  m11,map-from-meta,模型 meta 含 thinking_level_map,发起 generate_stream,options 含该 map,false
