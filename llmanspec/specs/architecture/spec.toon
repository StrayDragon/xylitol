kind: llman.sdd.spec
name: architecture
purpose: "架构约定 — 分层规则、依赖方向与 arch_guard 强制执行的模块组织。"
valid_scope[1]: src/AGENTS.md
requirements[12]{req_id,title,statement}:
  ar01,清除 pi 文档引用,src/ 中所有源文件 MUST NOT 在 doc comments 中引用 pi coding agent 或其 TypeScript 模块。pi 设计理由引用 MUST 替换为该模块实际职责的独立描述。
  ar02,组件粒度边界（修订：补上限）,"少于 100 行或 5 个方法且无行为逻辑的组件 MUST 内联到父 struct。反之，内联块超过约 100 行、有独立行为逻辑且概念内聚时 MUST 提取为命名协作者对象（见 agent-session.as30）。Agent 能力聚合（原 AgentSession）MUST NOT 作为单一 struct 或单一文件无界增长；export、bash、permission、stats、trust 关注点 MUST 提取为命名协作者，使聚合保持内聚。"
  ar03,宏系统评估文档,"项目 MUST 产出 docs/architecture/macro-registration.md，分析以 Rust proc-macro 或 declarative macro 替代运行时注册（tools、commands、providers）的可行性与 ROI。仅为设计评估 — 本变更不产出宏代码。"
  ar04,更新模块地图,"src/domain/ MUST 托管纯领域词汇与 consolidated entities；src/runtime_protocol/ MUST 托管 Xy 前缀 port 契约及其签名类型。"
  ar05,"app-layer-module-map","System MUST 更新模块地图：src/app/ 托管所有用户面入口（cli/、rpc.rs、server/、tui/、gui.rs）及 src/app/core/ 子层用于跨面 seam（composition、driver）；src/protocol/ 托管 client↔core wire 词汇，拆分为 command.rs/event.rs/transport.rs；src/app/core/composition.rs 集中 Agent 构造。"
  ar06,精选库入口 Xy 前缀,精选进 crate 公开 API（lib.rs pub use）的可替换端口、跨面生命周期事件及其绑定签名类型 MUST 使用 Xy 前缀；应用面缝（Driver/bootstrap/dispatch）、protocol 线协议类型、infra 具体实现、以及未导出的内部协作者 MUST NOT 仅为品牌而加 Xy 前缀。
  ar07,消除重复领域实体,"System MUST NOT 为同一领域概念定义多种类型；Usage、StopReason 与 Compaction 配置 MUST 各有一个 canonical 类型。"
  ar08,统一事件词汇表（闭集生命周期）,System MUST 以 XyEvent 作为 ReAct 输出流与生命周期总线的统一领域事件词汇；XyEvent MUST 保持为 agent 生命周期闭集（Agent/Turn/Message/Tool/Compaction/Queue 等），MUST NOT 为每个 provider 专属 SSE 事件新增变体。厂商流细节 MUST 在适配器边界映射为 XyChunk 或已有 MessageUpdate 载荷。
  ar09,精选 pub use 清单,System MUST 在 src/lib.rs（或等价公开入口）维护精选 pub use，至少覆盖 XyModel、XyTool、XySessionStore、XyEvent、XyChunk 及文档化的配套错误/配置类型；未列入清单的内部模块 MUST NOT 被误当作稳定公开 API。
  ar10,domain 不绑 schemars,"src/domain 下的领域类型 MUST NOT derive schemars::JsonSchema；需要 JSON Schema 的配置/settings DTO MUST 位于 infra/config 或 infra/settings（或等价配置边界）并在该处 derive。"
  "ar-embed1",公开嵌入装配缝,"System MUST 向库用户提供文档化的嵌入入口（含 bootstrap/Driver 或等价），使外部 crate 无需 reach-in agent::session 或 infra 具体类型即可装配并驱动一轮对话。"
  "ar-mcp-seam",MCP 配置经嵌入缝,"System MUST 向嵌入方提供不依赖 infra 路径的 MCP 服务器描述（或等价句柄），使 bootstrap/reload 可在不 reach-in infra::mcp 的情况下传递配置。"
scenarios[12]{req_id,id,given,when,then}:
  ar01,"pi-refs-cleared",src/ 文件含 'Aligns with pi' 注释,运行 rg 'Aligns with pi' src/,零匹配
  ar02,"aggregate-decomposed","Agent（原 AgentSession）已增至 25 字段，混合 export bash permission stats trust",变更已应用,这些关注点位于命名协作者对象，Agent 聚合保持内聚而非无界增长
  ar03,"macro-eval-doc","llmanspec/changes/c240-consolidate-architecture/ 已打开",运行 macro 评估任务,"docs/architecture/macro-registration.md 存在，含 #[tool]、#[command]、#[provider] 的 ROI 分析"
  ar04,"domain-map-updated",变更后检查 src/domain/,consolidated types 存在,无重复 CompactionConfig 或 XyFinishReason
  ar05,"app-layer-module-map-exists","",变更后检查源码树,"src/app 存在且含 core/ 子层，src/interactive 与 src/server 不再存在，src/protocol 为目录"
  ar06,"curated-export",准备精选 pub use,审查 lib.rs 导出列表,仅端口与 XyEvent/XyChunk 等契约类型带 Xy；Driver 与 AgentMessage 可不带
  ar07,"no-duplicate-usage","代码库搜索 token-usage 类型","rg \"struct (Usage|XyUsage)\"",恰好剩一个 canonical XyUsage
  ar08,"closed-set","",审查 XyEvent 变体与 provider 适配器,无厂商专名变体；流增量经 XyChunk 进入循环后再发标准 XyEvent
  ar09,"pub-use-exists","",检查 src/lib.rs,存在精选 pub use 且文档标明稳定契约
  ar10,"no-domain-jsonschema","",rg JsonSchema 于 src/domain,零匹配
  "ar-embed1","embed-surface","",审查 lib 公开 API,存在嵌入入口且文档列出稳定符号；Driver/bootstrap 可被 crate 外路径引用
  "ar-mcp-seam","no-infra-path","","审查 xylitol::embed 与 BootstrappedRuntime","公开字段/方法签名不出现 infra::mcp::McpServerConfig"
