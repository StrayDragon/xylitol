kind: llman.sdd.spec
name: "agent-session-store"
purpose: 会话持久化 — 基于 JSONL 文件的会话存储，含树操作、compaction、fork 与 CWD 校验。
valid_scope[1]: "infra 层 session 持久化"
requirements[31]{req_id,title,statement}:
  r31,快照操作,System MUST 支持对不可变会话快照执行 snapshot、restore、spawn、list、prune、diff、merge 操作。
  r41,compaction,"当 CompactionSettings.enabled 且上下文占用超过 window−reserve（c1630）时，System MUST 在 agent/capabilities 回合落定后自动 compact（c1640）；MUST NOT 仅依赖百分比闸或仅 TUI 轮询。"
  s1,JSONL 存储,SessionManager MUST 将会话持久化为 JSONL 文件（每行一个带版本标签的 JSON 对象），位于 ~/.xylitol/sessions/。
  s2,条目类型,"SessionManager MUST 支持条目类型（JSONL type 判别为 camelCase）：message、compaction、branchSummary、modelChange、thinkingLevelChange、custom、customMessage、label、sessionInfo；bang-bash 不作为顶层 type，而走 message+role=bashExecution。MUST NOT 将 snake_case type 标签（如 branch_summary、bash_execution）视为合法 SSOT。"
  s3,CRUD,SessionManager MUST 支持 create、append、load、list、exists 操作。
  s4,禁止旧 AgentPart 迁移,"Session JSONL 的 message.content AgentPart 形态以 domain-message dm1 为唯一真源；系统 MUST NOT 迁移或静默解读 c646 之前的 untagged/裸字符串 content。读到此类旧 content 时 MUST 按 agent-session as46 拒绝/跳过策略处理。文件级其它版本字段若存在 MAY 保留，但 MUST NOT 借此复活旧 AgentPart 语义。"
  s5,会话树,SessionManager MUST 维护 parent/child 会话链接：fork 创建子分支，branchSummary 引用父条目。
  s6,分支摘要,System MUST 支持 generateBranchSummary(parentEntries)，产出汇总切点前条目的 CompactionEntry。
  s7,写入安全,"SessionManager 写会话文件 MUST 满足崩溃原子性：进程任意时点终止后，盘上会话文件要么保持旧完整内容、要么呈现新完整内容，MUST NOT 出现截断或半行混合状态。系统以单进程单写者为并发假设（同一会话至多一个进程写入），MUST NOT 声称提供跨进程文件锁互斥。"
  s9,会话 fork,"SessionManager MUST 支持 fork(parent_id, child_id, at_entry_id)，将父条目复制到切点至新子会话文件并 append branch_summary 条目。"
  s10,分支摘要实现,fork 时 System MUST 生成描述被跳过条目的分支摘要：条目数、条目类型、最后 user 消息及工具调用中的 notable actions。
  s11,agent fork,System MUST 支持 fork_session(at_entry_id)：经 SessionManager::fork() 创建新子会话并返回子 session id。
  s16,会话 CWD 校验,从磁盘加载会话时 SessionManager MUST 校验存储 CWD 存在且可访问；不可用时返回可操作错误信息。
  s18,"session-entry-camelcase-v5","Session persisted JSONL MUST 以 camelCase 为唯一磁盘格式 SSOT（JS/TS favor；非 pi snake entry type）：外壳字段含 parentId、parentSession、firstKeptEntryId 等；SessionEntry type 判别为 message、compaction、branchSummary、modelChange、thinkingLevelChange、custom、customMessage、label、sessionInfo。新写入的 bang-bash MUST 使用 type=message 且 message.role=bashExecution，MUST NOT 再写出顶层 type=bashExecution 或 bash_execution。新写入的 header.version MUST 等于 SESSION_VERSION（6）。MUST NOT 再写出 parent_id 或 version≤5 作为新会话真源。MUST NOT 提供 serde alias、静默 v3/v4/v5 migrate、或将旧顶层 bash 提升为合法上下文。"
  s19,"tool-result-tool-call-id",role=toolResult 的 AgentMessage MUST 以 toolCallId 键持久化工具调用关联 id（对齐 pi）；MUST NOT 写出 toolUseId。content 内 MUST NOT 再嵌入 type=toolResult 的 AgentPart（工具结果只走独立 message 行）。
  s20,"session-load-skip-warn","load（及同源逐行解析）遇到无法按最新 SSOT 解析的行（坏 JSON、未知 type、非 SSOT snake type 如 bash_execution、旧 untagged content 导致无法投影）时 MUST 跳过该行并记录可观测 warn；同一 load/list 操作内明文 warn MUST 至多 3 条，超出后 MUST 以单条省略标记（如 ...）收敛，MUST NOT 刷屏。header.version 不等于 SESSION_VERSION（6）时 MUST 拒绝将该文件视为合法最新会话（返回可操作错误），MUST NOT 静默 migrate 后当成功。TUI resume、print/CLI --session 与 SessionManager MUST 共用此策略。"
  s21,"list-sessions-resilient","list_sessions（或等价枚举）遇到单个会话文件不可读/非最新/解析失败时 MUST 跳过该文件（或给出占位诊断）并继续枚举其余会话，MUST NOT 因单文件失败而使整表 list/resume 面板失败。"
  ex1,导出 HTML,System MUST 支持将会话导出为 HTML 文件。
  ex2,导出 JSONL,System MUST 支持将会话导出为 JSONL 文件。
  ex3,导入 JSONL,System MUST 支持从 JSONL 文件导入会话为新会话。
  ex4,HTML 工具渲染,HTML 导出 MUST 将 tool 与 assistant 内容渲染为可读块。
  ex5,分享指引,未配置 token 时调用分享，System MUST 返回配置指引。
  sc1,CWD 校验,恢复会话时 System MUST 校验会话头存储的 cwd 在磁盘可用；不可用时 MUST 尝试 fallback cwd，其一可用即继续。
  sc2,CWD 错误,两者均不可用时 System MUST 返回可操作校验错误，消息 MUST 同时携带存储 cwd 与 fallback cwd。
  sc3,CWD 呈现,面向用户的 CWD 错误信息 MUST 即该可操作校验错误文本（引导用户落到 fallback cwd）。
  sc4,CWD 集成,CLI 与 RPC 模式 MUST 在恢复会话前完成同一校验。
  sp1,SessionStore 端口实现,"infra 层的 SessionManager MUST 实现 protocol 的 XySessionStore 端口（append、load、load_context、exists），仅覆盖 agent 循环与 compaction 所需；完整会话面（fork、navigate、export）MAY 留在具体结构体供组合根直接使用。"
  sp6,日志访问,"会话 store MUST 暴露 read_recent(session_id, limit) -> Vec<Event> 供 server journal；journal 容量 MUST 可配置（默认 10000）。"
  sp3,ExportIo 实现,System MUST 提供导出 I/O 端口实现（StdExportIo 或等价，tokio::fs 读写）；组合根 MUST 将导出协作器经该端口注入。
  s22,磁盘格式时间戳,"Session 磁盘格式时间戳（header.timestamp 与条目壳 timestamp）MUST 为 u64 unix 毫秒数，与 message 内 timestamp 同基准；MUST NOT 写出 RFC3339 字符串作为磁盘格式时间戳。展示面需要人类可读时间时 MUST 在展示边格式化。"
  s12,延迟持久化至 assistant,Persisted SessionManager（非 in_memory backend）MUST 对齐 pi：首条 assistant 消息写入前，会话条目可仅保留在内存；首条 assistant append 时 MUST 创建磁盘 jsonl（若尚不存在）并刷出此前挂起条目（含 header 与先前 user 等）；其后每次 append MUST 落盘。InMemory backend MUST NOT 写文件。MUST NOT 在仅有 header、用户又放弃对话时强制留下无消息垃圾文件作为唯一策略（允许 create 仍写 header，但 deferred 刷出路径 MUST 存在并可测）。
scenarios[36]{req_id,id,given,when,then,feature}:
  s18,session-entry-camelcase-v5-unit,"写入新会话 JSONL","检查外壳字段与 header.version","camelCase（parentId/parentSession/type）且 version=6；bang-bash 为 message+role=bashExecution；无顶层 bash",false
  s19,tool-result-tool-call-id-unit,"持久化 role=toolResult 消息","serde JSONL","含 toolCallId 且无 toolUseId；content 无嵌入 toolResult AgentPart",false
  s20,session-load-skip-warn-unit,"JSONL 含 bash_execution 与未知 type 行","load 会话","坏行被跳过；warn≤3 后出现省略；合法行仍可用",false
  s21,list-sessions-resilient-unit,"sessions 目录含一损坏 jsonl 与一合法会话","list_sessions","返回含合法会话；不因损坏文件整表失败",false
  s7,write-crash-atomicity-unit,"存在既有会话文件","重写路径（flush merge / header repair）中途进程终止","盘上文件保持旧完整内容或新完整内容；无截断或半行混合",false
  s22,session-timestamps-unix-ms-unit,"v6 会话文件","读取 header 与条目壳 timestamp","均为 u64 unix-ms 整数；无 RFC3339 字符串",false
  r31,happy,存在快照,以 snapshot_id 与新 prompt 调用 spawn,新 agent 实例以快照上下文启动,false
  r41,happy,对话超过上下文窗口 75%,触发 compaction,较早回合被摘要并替换为 compact system message,false
  s2,types,append 各类型条目,加载会话,各条目保留类型与数据,false
  s3,list,存在多个会话,调用 list(),返回全部 session id,false
  s4,no-untagged-migration,磁盘 JSONL 含旧 untagged thinking 对象,load 并解析为 AgentPart,不迁移为合法 Thinking；走拒绝或跳过,false
  s6,branch-summary,切点前有 5 条条目,调用 generateBranchSummary,产出汇总该 5 条的 CompactionEntry,false
  s7,write-atomicity,存在既有会话文件,重写路径（flush merge / header repair）执行后进程任意时点终止,盘上文件保持旧完整内容或新完整内容；无截断或半行混合；同一会话至多一个进程写入,false
  s9,basic-fork,会话 A 有 e0 至 e9 共 10 条,在 e4 fork,子会话有 5 条（e0..e4）加 branch_summary,false
  s9,full-fork,会话 A 有 5 条,在最后条目 e4 fork,子会话含全部 5 条且无 branch_summary,false
  s9,parent-link,经 fork 创建子会话,加载子会话 header,parent_session 字段匹配父 id,false
  s10,summary,父会话有 3 条 user、7 条 assistant 与 5 次工具调用,生成分支摘要,摘要含总条目数与工具调用数,false
  s10,empty-summary,fork 点后无剩余条目,生成分支摘要,摘要为空串或表明无跳过内容,false
  s11,agent-fork,agent 会话活动,调用 fork_session(e4),返回新子 session id 且磁盘存在子会话文件,false
  s16,cwd-ok,会话文件 cwd 为 /tmp 且目录存在,调用 load,正常返回条目,false
  s16,cwd-missing,会话文件 cwd 为 /nonexistent,调用 load,错误引用缺失目录 /nonexistent,false
  ex1,html,会话有消息,调用 export_to_html,写入 HTML 文件,false
  ex2,jsonl,会话有条目,调用 export_to_jsonl,文件每行一个 JSON 对象,false
  ex3,import,存在合法 JSONL 文件,调用 import_from_jsonl,条目加载到新会话,false
  ex4,html-tools,会话含工具调用,打开导出 HTML,工具结果渲染为可读块,false
  ex5,share-unconfigured,未配置 token 调用 share,调用 share,返回配置指引消息,false
  sc1,exists,存储 cwd 在磁盘存在,恢复会话,校验通过,false
  sc1,fallback,存储 cwd 不存在但 fallback 可用,恢复会话,回退 fallback 继续,false
  sc2,cwd-error,存储 cwd 与 fallback 均不可用,恢复会话,返回校验错误且消息携带两个路径,false
  sc4,cli-missing,CLI 启动时会话 cwd 缺失,展示校验错误文本,引导用户至 fallback cwd,false
  sp1,manager-impls-store,对照 XySessionStore 端口检查 infra 层 SessionManager,检查端口实现,append、load、load_context、exists 可编译且往返数据,false
  sp6,journal-reads-recent,会话有 50 个事件且 journal limit 为 10000,"调用 read_recent(s0, 10)",按 seq 顺序返回最后 10 个事件,false
  sp3,std-export-io,组合根为导出构造 Agent,注入 ExportIo,具体类型为 infra/ 的 StdExportIo,false
  s12,defer-before-assistant,新 persisted session 仅有 user 消息已 append,检查磁盘 jsonl,文件尚不存在或未含完整挂起条目刷出；首条 assistant append 后文件存在且含 user+assistant,false
  s12,in-memory-no-file,"SessionManager::in_memory()",append 多条消息,无 session 文件被创建且 load 仍可读到条目,false
