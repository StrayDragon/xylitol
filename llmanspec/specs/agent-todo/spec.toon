kind: llman.sdd.spec
name: "agent-todo"
purpose: 会话内 Todo SSOT — Custom latest-wins 持久、内置三工具读写、至多一条 in_progress、不进 LLM 前缀；TUI 可折 checklist 与状态栏只读投影边界。
valid_scope[4]: "protocol 层","agent 层","infra 层工具","产品 TUI 面"
requirements[12]{req_id,title,statement}:
  atd1,"todo-domain-model","会话 Todo 列表 MUST 为有序条目集合；每条含稳定 id、非空 content（trim 后）、status ∈ pending|in_progress|completed|cancelled。空表合法。completed/cancelled MUST 保留在表内直至整表 rewrite 删除。本波 MUST NOT 要求 max_attempt、assignee、namespace 或 plan 类字段。"
  atd2,"custom-agent-todo-latest-wins","Todo 真源 MUST 以会话 Custom 条目持久化：custom_type 为 agent_todo，载荷为当前全量 items 快照。每次成功变更 MUST append 一条完整快照；读取 MUST 在当前 leaf 分支上 latest-wins（全叶扫描，MUST NOT 仅依赖裁切后的 LLM context 窗）。MUST NOT 另开旁路文件当真源；MUST NOT 用会进模型前缀的 CustomMessage/session_env 冒充 Todo SSOT。"
  atd3,"todo-not-in-llm-prefix","agent_todo Custom 快照 MUST NOT 经会话→模型投影进入 provider 输入前缀（含 system / session_env / 状态栏族标签）。模型可见 Todo MUST 仅来自工具结果或模型主动调用 todo_list。由单测覆盖；MUST NOT 为静态存在性单独扩 BDD step。"
  atd4,"todo-tools-semantics","产品默认工具表（Print 与 TUI 共用 builtins 基座）MUST 提供三工具：todo_list（只读返回当前全表）、todo_rewrite（整表替换后返回全表）、todo_update（按 id 改 status 与/或 content，至少改一项；成功返回全表）。未知 id MUST 拒绝并返回可读错误。成功写入后 MUST 与 atd2 快照一致。本波 MUST NOT 要求 filter 参数。"
  atd5,"at-most-one-in-progress","任一合法 Todo 快照中 in_progress 条目 MUST 至多一条。todo_rewrite / todo_update 若会导致两条及以上 in_progress，MUST 拒绝写入并返回可读错误，且 MUST NOT append 新快照。"
  atd6,"todo-tool-concurrency-barrier","todo_list / todo_rewrite / todo_update MUST 为 Barrier（或等价 Sequential）并发类，与其它会改会话状态的内置工具同族；调度 MUST 尊重该类。分类表由单测覆盖，MUST NOT 为静态表单独扩 BDD step。"
  atd7,"status-bar-read-only-boundary","Todo SSOT 为本波唯一业务真源。日后状态栏/Agent 列若投影完成数等摘要，MUST 只读自本 SSOT；MUST NOT 以栏 auto 摘要写回 Todo；MUST NOT 要求本波实现栏注入、Agent 列 publish 或 refresh 工具。禁止 todo_* 只写栏不写 SSOT。"
  atd8,"tui-checklist-collapsible","产品 TUI 在有 Todo 条目时 MUST 以对话区可折叠 checklist 呈现同源列表：默认一行摘要（含完成/总数类计数，如 Todo · 2/5）；用户展开见完整只读勾选列表。MUST NOT 以常驻 Plan/双栏侧栏、status/footer/chrome toast 或 ScrollNotice 刷墙作为主清单。本波 MUST NOT 要求用户手改条目或 /todo slash。"
  atd9,"tui-resume-and-tool-refresh","TUI 在 resume / 切换会话后 MUST 自当前 leaf 上 latest agent_todo 快照重建 checklist；todo_* 工具成功结束后 MUST 刷新为与 SSOT 一致的摘要/列表。Print 面 MUST NOT 要求 checklist UI，但工具与持久 MUST 仍可用（同源 SSOT）。"
  atd10,"compact-preserves-todo-snapshot","压缩或裁切会话上下文时，若操作会丢掉当前 leaf 上全部 agent_todo 快照，System MUST 在裁切后重新 append 最新全量快照，使后续 todo_list / TUI / resume 仍可读到同一逻辑列表。MUST NOT 静默永久丢弃用户 Todo。"
  atd11,"todo-builtins-first-turn-freeze","todo_list / todo_rewrite / todo_update 属核心 builtins，MUST 在会话首次工具表定稿前进入可见工具表；MUST NOT 在回合中途热加这三个名字。与既有首轮冻表门闸一致；由单测覆盖，MUST NOT 单独扩 BDD step。"
  atd12,"export-shows-agent-todo","会话导出（HTML/JSONL 或等价）MUST 能呈现 agent_todo Custom 快照（或等价标记），MUST NOT 将其伪装成用户消息。"
scenarios[14]{req_id,id,given,when,then,feature}:
  atd1,"domain-status-closed-set",构造合法 Todo 条目,检查 status 枚举,仅 pending|in_progress|completed|cancelled,false
  atd2,"latest-wins-full-scan",同一 leaf 连续 append 两条 agent_todo,读取当前列表,等于最后一条全量 items,false
  atd3,"custom-skipped-in-llm-prefix",历史含 agent_todo Custom,project_for_llm 或等价投影,前缀无 Todo 清单正文,false
  atd4,"rewrite-returns-full-list",调用 todo_rewrite 合法 items,检查 tool result 与 store latest,二者 items 一致且为全表,false
  atd4,"update-unknown-id-rejects",对不存在 id 调用 todo_update,执行,可读错误且无新快照,false
  atd5,"dual-in-progress-rejects",rewrite 或 update 将产生两条 in_progress,执行,拒绝并保持原 latest,false
  atd6,"todo-tools-barrier-class",查询三工具并发类,对照分类表,均为 Barrier 或 Sequential,false
  atd7,"bar-must-not-write-todo",仅存在栏摘要投影意向,检查写路径,无栏→SSOT 写回,false
  atd8,"checklist-default-summary",列表非空且未展开,外显 TUI,仅一行含计数摘要且无侧栏主清单,false
  atd9,"resume-rebuilds-checklist",落盘已有 agent_todo 后 resume,打开 TUI checklist,摘要与 latest 一致,false
  atd10,"compact-reappends-todo",裁切将去掉全部 agent_todo,compact/cut 完成,leaf 上仍有等价最新快照,false
  atd11,"todo-names-frozen-first-turn",含 Todo 的核心表首次定稿后,回合中试图热加同名,provider 可见表不变,false
  atd12,"export-marks-custom-todo",导出会话含 agent_todo,检查导出物,可见 custom/agent_todo 标记且非用户消息,false
  atd4,"todo-list-readonly",调用 todo_list,检查副作用,无新 Custom 快照且返回当前全表,false
