kind: llman.sdd.spec
name: "runtime-config"
purpose: 运行时配置 — 深合并、secret.env、settings；仅 config.yaml + secret.env，不支持 config.local。
valid_scope[2]: src/infra/config/,src/infra/settings/
requirements[16]{req_id,title,statement}:
  rc0,占位,System SHALL 支持完整 pi Settings 接口，含 camelCase JSON 序列化。
  rc11,transport 字段,Settings MUST 含 transport 字段，枚举 auto、sse、direct。
  rc12,mode 字段,"Settings MUST 含 steering_mode 与 follow_up_mode，均为 one-at-a-time 枚举选项。"
  rc13,杂项字段,Settings MUST 含 shell_path、shell_command_prefix、npm_command、default_project_trust、enable_skill_commands、session_dir、markdown、warnings 及 c83 的 http 相关字段。
  rc14,列表字段,Settings MUST 含 prompts 与 themes 作为 Option<Vec<String>> 用于资源路径配置。
  rc15,compaction 配置映射,运行时配置 MUST 定义从 YAML 加载期 compaction 表面到规范运行时 compaction settings 的单一文档化映射，无并行重复类型。
  rc1,ConfigValue 在 infra,"ConfigValueResolver（环境变量、shell 命令与 ${VAR:-default} 解析，原 agent/config_value.rs）MUST 位于 infra/config/value.rs；除 infra/config 外 MUST 零 crate:: 内部依赖；agent/ MUST NOT 含 config_value.rs。"
  rc8,配置边界承载 JsonSchema,需要对外暴露 JSON Schema 的配置结构 MUST 在 runtime/config 边界（infra/config 或 settings）定义或包装，并 MUST 能映射到 domain 纯数据（若存在对应领域类型）。
  rc9,mcp_servers 配置字段,运行时配置 MUST 提供 mcp_servers（或等价）字段以声明 MCP 服务器；缺省或空表示未启用 MCP。
  rc16,"model-entry-thinking-levels",ModelsConfig 的 ModelEntry（或等价 YAML 面）MUST 支持可选 thinking_levels 字符串列表；解析时未知 level 名 MUST 使配置加载失败；Settings.default_thinking_level 若存在 MUST 可被选模/启动路径消费（合法则采用，非法则忽略并 clamp）。
  rc17,"model-entry-thinking-level-map",ModelsConfig 的 ModelEntry MUST 支持可选 thinking_level_map（键为 ThinkingLevel 名，值为字符串或 null）；未知键名 MUST 使配置加载失败；解析结果 MUST 进入 XyModelMeta（或等价）供请求组装；键缺省 MUST 表示使用 adapter 内置默认，null MUST 表示该档不向 provider 发送 thinking/effort 字段。
  rc18,"model-entry-tokenizer","ModelsConfig 的 ModelEntry MUST 支持可选字符串字段 tokenizer：可为顶层 tokenizers 表中的名字、HF owner/repo、本地路径或 builtin；AppConfig MUST 支持可选顶层 tokenizers 映射（条目含 repo/file 或 path）供多模型共用同一词表源；缺省 tokenizer 时 MUST 回退 registry builtin 启发式或视为未映射，MUST NOT 用推理用 model id 静默假定 HF 仓库。"
  rc19,"token-estimate-local-tokenizer","AppConfig MUST 支持可选 token_estimate.local_tokenizer，取值仅 on 或 off（或缺省等价 off）；默认 MUST 为 off；非法值 MUST 使配置加载失败；该闸 MUST 接到估计路径的 LocalTokenizer 允许位（paa10），MUST NOT 引入其它本地计数策略枚举。"
  rc20,"config-yaml-secret-env-only","产品配置 MUST 仅以 config.yaml（可分享非密钥）与 secret.env（密钥；YAML 经 {{ secret.KEY }}）为配置面；加载链 MUST 为 global config.yaml → project config.yaml → 可选 --config，外加 secret.env 注入；AppConfig 全局目录 MUST 以 ~/.config/xylitol/ 为 SSOT，~/.xylitol/ MUST 定位为数据目录（sessions/tokenizers/logs 等），MUST NOT 鼓励在 ~/.xylitol/config.yaml 放置 AppConfig（migrate 兼容除外）。"
  rc21,"config-local-unsupported","加载器 MUST NOT 读取或深合并 config.local.yaml 或 config.local.yml（global 与 project）；磁盘上若存在这些文件 MUST 忽略其内容且 MUST NOT 提供自动迁移到 config.yaml/secret.env；MAY 经 resources doctor 或加载诊断提示已忽略。"
  rc22,"no-agent-max-iterations","Agent profile / AppConfig MUST NOT 暴露或生效 max_iterations（或等价硬步数闸）配置字段；schema 与加载路径 MUST 一次性移除该旋钮且 MUST NOT 提供向后兼容 shim；残留键 MUST NOT 再限制 ReAct 迭代。由配置单测或加载用例覆盖，MUST NOT 为静态字段缺失单独扩 BDD step。"
scenarios[1]{req_id,id,given,when,then,feature}:
  rc22,"profile-no-max-iterations",Agent profile 类型与 schema,检查 max_iterations 字段,MUST NOT 存在可生效字段,false
