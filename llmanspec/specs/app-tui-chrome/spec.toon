kind: llman.sdd.spec
name: "app-tui-chrome"
purpose: 主题 token、glyph、status 与 footer 等 layout 壳（历史 capability 名 chrome）。
valid_scope[1]: src/app/tui/
requirements[21]{req_id,title,statement}:
  atc1,"idle-status-omitted",产品 UiRoot 空闲时 status 槽 MUST 渲染 0 行；忙碌时 MUST 至多独立 1 行短状态；MUST NOT 将 Working 等忙碌文案并入 footer。
  atc2,"footer-minimal","产品 footer MUST 为 1 行 dim 文本，显示 active（in-flight 或当前 turn 已绑定）的 model，而非仅裸读 selected；字段序为 cwd · model · thinking 标签（形如 `cwd · model · thinking off` 或 `· xhigh`，level 文案为 ThinkingLevel::as_str，off 时用 `thinking off`；字段间只用 ` · `，MUST NOT 在 thinking 前再加装饰 `•`）；当 active 模型无思考或仅不可调 off 时 MUST 省略 thinking 段；并在可获得 ContextTokenEstimate 时追加 `· used … tokens`（文案按 TokenProvenance：Api/RemoteCount/LocalTokenizer 为 used N tokens，Heuristic 为 used ~N tokens，Unknown 为 used ? tokens）；当当前模型 context_window > 0 时 MUST 在 used 字段后追加派生占用比（见 atc21）；队列非空时可加 `q:sN|fM ·` 前缀；无估计数据时 MUST NOT 伪造 used 0；MUST NOT 常驻快捷键墙；放不下时 MUST 截断右侧而非增高。"
  atc3,"theme-glyphs-config","产品 host MUST 经 Palette::dark()（或等价闭包）将语义色注入组件主题；glyph 档（unicode/ascii）MUST 由显式配置切换，MUST NOT 做运行时字体探测；产品 MVP MUST NOT 默认开启 theme auto。"
  atc4,"design-docs-ssot","产品 TUI 视觉与 UX MUST 以 src/app/tui/DESIGN.md 为索引、以 src/app/tui/design/*.md 为组件级 SSOT；实现与 demo 引用组件规则时 MUST 指向对应子文档（尤其 design/diff-block.md 与 design/keybindings.md），MUST NOT 仅依赖口头约定。"
  atc5,"demo-theme-auto-detect",agent_demo MUST 默认使用 Dark token 集；当启用主题自动探测（环境变量或 harness API）时 MUST 经 package terminal_colors 纯函数切换 Light/Dark，并在 layout 壳（footer 或系统行）暴露当前 theme_mode；MUST NOT 将自动切换设为产品 MVP 默认。
  atc6,"idle-editor-compact",产品空闲且 Editor 草稿为空时操作区可见内容行 MUST 紧凑（对齐 agent_demo 观感：上下 ─ 保留，避免大块空盒）；有多行草稿时 MUST 仍可按既有 terminal_rows/max_vis 规则长高。
  atc7,"busy-status-spinner","产品 agent-busy status 行 MUST 为至多一行：左侧 lead 为 accent spinner（或等价动画帧）加短词（MUST 作为同一组合贴左，MUST NOT 用 space-between 拆开 spinner 与短词）；idle MUST 仍为 0 行；MUST NOT 把 spinner 画进 footer；pending trail 见 atc19。"
  atc8,"user-message-bg","产品 scrollback 用户消息行 MUST 可应用 DESIGN user-message-bg 全行淡底（默认开启或与 agent_demo 一致）；MUST NOT 改变消息文本语义前缀。"
  atc9,"compaction-retry-single-status-line",产品 TUI 在 Compacting 或 Retry n/m 忙碌态时 status 槽 MUST 仍至多独立 1 行（spinner+短词）；压缩/重试细节 MUST 走 scrollback System 行而非增高 status；MUST NOT 常驻多行 compaction/retry layout 条。
  atc10,"abort-status-idle",产品在用户 abort 后 status 槽 MUST 回到 0 行（idle）；取消说明 MUST 走 scrollback System 一行而非增高 status；bang 执行中 MAY 使用单行 Running。
  atc11,"models-picker-footer","经 models 槽或 /model <id> 成功更新 selected 后：idle 时 footer model/thinking MUST 反映新 active（与 selected 收敛）；agent-busy 时 footer MUST 仍显示 active（旧绑定），pending 仅出现在 status trail（atc19）。无思考模型 MUST 省略 footer thinking 段。"
  atc12,"status-spacer-above-editor",产品 TUI status 槽：busy 时 MUST 渲染 Loader 前导空行加一行 spinner 短词并紧贴 editor 上方（MUST NOT strip 空行）；idle 时 MUST 保留恰好一行空白呼吸间距（MUST NOT 显示 Ready 文案）；对齐 agent_demo status_lines 与 status.md。
  atc13,"footer-token-provenance","当 footer 展示上下文 token 占用时，文案 MUST 反映 Driver::estimate_context_tokens 返回的 TokenProvenance：精确来源（Api/RemoteCount/LocalTokenizer）MUST 使用无波浪号的 used N tokens；Heuristic MUST 使用 used ~N tokens；Unknown MUST 使用 used ? tokens；MUST NOT 将 Heuristic 显示为无 ~ 的精确值；派生占用比的波浪号规则见 atc21。"
  atc14,"footer-token-refresh","产品 host MUST 在 session tree travel 换叶、一轮 agent turn 结束后回到 idle、CompactionEnd、TurnEnd、以及 turn 进行中有可用 Api usage 更新时（节流）刷新 footer token 字段；刷新 MUST 经 Driver 只读 estimate_context_tokens（或等价 seam）且异步不阻塞输入；MUST NOT 在每个 TextDelta 上全量本地 tokenizer.encode。"
  atc15,主题热应用,产品 TUI MUST 能按主题名（至少内建 dark/light）将 Palette 应用到 LayoutTheme/UiRoot；未知或非法名 MUST 保留旧主题并报告诊断；重载 MUST NOT 清空 transcript。
  atc16,"theme-slash-slot","产品 /theme 无参 MUST 使用独立 editor 槽（Themes SelectList，对齐 Models 槽模式）挂载内建主题项；选中 MUST 调用已有主题热应用路径（atc15）并关闭槽；Esc MUST 关闭槽且 MUST NOT 改主题；MUST NOT 解冻 Plate/Settings/Choice 活板；MUST NOT 将 theme auto 或 demo Ctrl+P theme-toggle 设为产品默认。"
  atc17,"thinking-border-and-footer",产品 TUI MUST 将当前 Driver thinking level 映射为 ThinkingBorderLevel 并经 apply_thinking_border（或等价）染色 Editor 操作区边框；footer MUST 同步显示对应 level 标签（见 atc2）；主题切换后 MUST 按新 Palette 重涂 thinking 边框；bash 强调色边框（ati15）开启时 MUST 覆盖 thinking 边框，退出 bash 前缀后 MUST 恢复当前 thinking 边框而非仅 muted。
  atc18,"loaded-resources-slot","产品 UiRoot MUST 在 scrollback 上方渲染 loaded-resources 槽：Codex 风边框卡片（>_ xylitol + model/directory；有 skills 时 skill-ref 色 skills 行并换行全量；有 MCP 时 success 色 mcp 行）；MUST NOT 展示木糖醇中文标签；MUST NOT 渲染 ASCII logo 艺术字；MUST NOT 用省略号截断资源名；MUST NOT 列出 prompt templates；MUST NOT 展示密钥或完整 env。"
  atc19,"status-trail-pending","产品 agent-busy 且 selected ≠ active 时，status 行右侧 MUST 显示 trail：模型 pending 优先文案 `Next turn: {model}`；仅 thinking pending 为 `Next turn thinking: {level}`；MUST NOT 拼接为 `Next turn: model · high`。trail MUST 右对齐（margin-left auto 或等价），MUST NOT 破坏 lead 左贴。selected==active、NextTurn 消费、idle 或 abort 收敛后 MUST 清除 trail。仅 bang busy（无 agent LLM run）MUST NOT 显示 Next-turn trail；footer 可跟随 selected。status 短词轮换（Working/Compacting 等）MUST 保留 trail。"
  atc20,"chrome-success-no-system","产品 host 成功切换 model、thinking 或 theme 时 MUST NOT 向 scrollback 追加成功确认类 System 行（如 model → …）；失败与诊断 MAY 写 System。"
  atc21,"footer-derived-context-percent","当 footer 已展示 ContextTokenEstimate 且当前模型 context_window > 0 时，MUST 在 used 字段后追加派生占用比：percent = tokens/context_window*100（展示 1 位小数）与紧凑 window（如 128k），形如 ` · 32.8%/128k`；Heuristic MUST 为 ` · ~p%/W`；Unknown 且有窗 MUST 为 ` · ?%/W`；context_window 为 0 或不展示 used 字段时 MUST NOT 追加 %；该百分比 MUST 仅作展示，MUST NOT 作为 should_compact / reserve 触发 SSOT，MUST NOT 恢复 compaction_threshold 或百分比闸配置。"
scenarios[0]:
