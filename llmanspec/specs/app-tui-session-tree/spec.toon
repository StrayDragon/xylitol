kind: llman.sdd.spec
name: "app-tui-session-tree"
purpose: 产品面双 Esc 会话树 stub（c491 冻结）：仅槽替换与 travel 行；活树/真 Driver 等开闸。形态学见 agent_demo（ast3/ast4）。
valid_scope[1]: src/app/tui/
requirements[15]{req_id,title,statement}:
  meta0,"capability-shell","本 capability 由变更 c491-add-app-tui-session-tree 引入；归档合并后以 delta 增补的 requirements 为准。本壳 MUST 保持非空以满足 BDD 护栏。"
  ast1,"slot-replace",会话树打开时 MUST 替换 UiRoot editor 槽（贴底可见）；MUST NOT 画成内容区顶部 overlay。
  ast2,"live-travel-via-driver","产品双 Esc 会话树 MUST 渲染当前 session 的 MessageHistory 活树（经 Driver::session_tree(MessageHistory) 映射为包 TreeNode，含 kind）；Enter 选中节点时 MUST 调用 Driver::travel_session_tree(MessageHistory, id)，按返回的 SessionTreeTravel 关闭树、预填 editor（仅当 editor_text 有值）、并重建/刷新 transcript 与后续提交 leaf 语义；MUST NOT 再使用 c491 假树样例作为唯一数据源；filter / 增量搜索产品接线见 ast8；fold / Shift+F fork 仍属后续 change。"
  ast3,"demo-live-session-tree","在产品真 session 图接线前，packages/xylitol-tui agent_demo MUST 维护可变 session_tree：用户提交、工具事件与助手回复结束 MUST 在当前 history leaf 下挂对应节点并推进 leaf；双 Esc 打开的树 MUST 渲染该活树而非仅静态样例。"
  ast4,"demo-travel-pi-semantics",agent_demo 在会话树 Enter travel 时：若选中节点 kind 为 user，则 MUST 将 history leaf 设为该节点的父（根 user 则回到无叶/约定根策略）、MUST 用该 user 正文预填编辑器（可剥 steer 前缀）、MUST 重建 transcript 为 root→父 路径且 MUST NOT 纳入被选 user 及其后线性回复；若选中非 user，则 MUST 将 leaf 设为选中 id、MUST 重建 root→选中路径、MUST NOT 因 travel 预填 user 正文；完成后 status MUST 回到空闲（无 spinner）。
  ast5,"demo-session-tree-fork","agent_demo 会话树打开时 MUST 支持 Shift+F fork：重建 root→选中节点路径（MUST NOT 自动纳入其后线性 assistant/tool 回复链）；history leaf MUST 等于选中 id；若选中为 user 消息 MUST 将其文本预填编辑器；关闭树后下一次用户提交 MUST 在该 leaf 下挂新子节点（与既有子树形成兄弟分叉）；MUST NOT 打开新会话文件或调用产品 Driver::fork。"
  ast6,"map-session-tree-nodes","产品 MUST 将 domain SessionTreeNode（MessageHistory）映射为 xylitol-tui TreeNode：id 取 entry id、label 为可显示正文（MUST NOT 把 role 烘焙进 label）、kind 取 message role 或等价（user/assistant/tool 等）；SessionTreeNode.label 有值时 MUST 映射为 TreeNode.annotation；bookkeeping 类 entry（ModelChange / ThinkingLevelChange / Label / SessionInfo / Custom / CustomMessage / Header）MUST 映射 kind=meta 以便 default 过滤；主题 kind_prefix 继续由 LayoutTheme 供给。"
  ast7,"tree-reflects-persisted-turn","在 auto-persist 与稳定 session_id 就绪后，产品双 Esc 打开的 MessageHistory 树 MUST 能反映已完成回合写入 store 的消息节点；一轮 user/assistant 成功结束后 session_tree(MessageHistory) MUST NOT 因 store 仅有 header 而长期为 0 节点（空会话除外）。"
  ast8,"session-tree-filter","产品会话树打开时 MUST 支持与 pi 对齐的 FilterMode（default / no-tools / user-only / labeled-only / all）：经 TreeSelector include_node 应用谓词，并与包增量搜索 AND；default MUST 隐藏 kind=meta；no-tools MUST 在 default 基础上隐藏 kind=tool；user-only MUST 仅 kind=user；labeled-only MUST 仅 annotation 有值；all MUST 显示全部；状态行 MUST 在非 default 时追加 [no-tools]/[user]/[labeled]/[all] 之一，default MUST NOT 追加 [default]；MUST NOT 在 packages/xylitol-tui 硬编码产品 FilterMode。"
  ast9,"session-tree-fold","产品会话树打开时 MUST 将 Ctrl+Left / Alt+Left / Ctrl+Right / Alt+Right（包 tui.tree.foldOrUp / unfoldOrDown）转发给 TreeSelector::handle_input，以折叠/展开可折节点或执行分支跳转；折叠后 MUST 隐藏该节点后代并显示 ⊞（或包等价标记）；MUST NOT 在 src/app/tui 重写 folded_nodes 算法；裸 Left/Right MUST 仍为翻页（既有）。"
  ast10,"product-session-tree-fork","产品会话树 Shift+F MUST 创建新 child session：内容 MUST 为 get_branch 路径并重链 parent_id（对齐 pi createBranchedSession），MUST NOT 按 JSONL 文件序切片，MUST NOT 改写父文件，header MUST 含 parent_session。选中 user 时 MUST 用 ForkPosition::Before（leaf=parent、预填正文、不拷该 user）；选中非 user 时 MUST 用 ForkPosition::At（路径含选中、不因 fork 预填 user 正文）。随后 MUST switch_session 到 child 并关树。MUST NOT 使用 demo 同会话 ast5。"
  ast11,"session-tree-slot-help-search","产品会话树槽打开时 MUST 在 TreeSelector 列表上方渲染 Search 行与 TreeHelp 行：Search 在无查询时提示可键入搜索、有查询时显示 Search: 与当前串；TreeHelp MUST 根据 KeybindingsManager（或只读封装）解析当前键位展示 move/page/fold/unfold（折叠）/filters/cycle 等用途片段（可用 · 分隔并随宽度换行），MUST NOT 再使用仅含 Up/Down Enter travel 的过时硬编码唯一提示行作为树槽唯一头行；MUST NOT 把产品 FilterMode 硬编码进 packages/xylitol-tui。"
  ast12,"session-tree-label-persist","产品会话树打开时 Shift+L MUST 进入节点 annotation 编辑；Enter 提交后 MUST 经 Driver 将 Label entry 写入当前 session（空串 MUST 清除 annotation）；重开或刷新后 MUST 仍可见对应 TreeNode.annotation；Shift+T MUST 切换 annotation 时间戳显示（本 change 允许本地 just now）；MUST NOT 在产品面 reach infra::session。"
  ast13,"debug-scene-tree-fixture","经 /debug session-tree-multiturn 或 /debug session-tree-labeled 装载后，产品双 Esc 打开的 MessageHistory 树 MUST 非空且可见 fixture user 正文；session-tree-labeled MUST 使至少一节点 annotation 有值以便 labeled-only 过滤可验。"
  ast14,"session-tree-e2e-pointer","产品会话树 Search/Help 与 Shift+L 行为 MUST 有 tests/tui_e2e 产品 Fake PTY 覆盖（见 test-qa-gate qg05）；本 requirement 为验收指针，不另增运行时行为。"
scenarios[26]{req_id,id,given,when,then}:
  meta0,"shell-present","","列出 llmanspec/specs/app-tui-session-tree",spec.toon 存在且 purpose 为中文
  ast1,"double-esc-opens",空编辑器,双 Esc 于 500ms 内,editor 槽显示 Session tree
  ast2,"open-shows-live",Driver 当前 session 已有 user/assistant 消息,空编辑器双 Esc 打开树,树行来自 MessageHistory 活数据而非 sample_session_tree 假树
  ast3,"submit-grows-tree",agent_demo 已打开,提交唯一 user 文本,session_tree 含该文本且再开树可见
  ast3,"tool-grows-tree",agent_demo 脚本轮产生 Tool,推进 tick 至工具出现,"session_tree 含 tool: 节点"
  ast4,"travel-user-prefills-editor",样例树选中 u1（kind=user，其下有 assistant 子节点）,Enter travel,编辑器含原 user 正文、leaf 为 u1 的父、transcript 不含 u1 子 assistant 正文、树关闭且 status 空闲
  ast4,"travel-user-omits-reply-spine",同上,Enter travel,transcript 不含 travel 前跟在 u1 后的线性 assistant/tool 回复
  ast4,"travel-assistant-no-prefill",选中 assistant 节点,Enter travel,leaf 为该 assistant id 且编辑器未因 travel 填入 user 正文
  ast5,"fork-stays-on-user",树选中样例 u1（其下已有 assistant 子节点）,Shift+F 或 fork_from_history,leaf 为 u1、编辑器含原 user 文案、transcript 不含子 assistant 正文
  ast5,"submit-creates-sibling",fork 到 u1 之后,提交新 user 文本,u1 的 children 数量加一且活树含新文案
  ast6,"annotation-mapped",活树节点域 label 已 resolve,打开会话树,对应 TreeNode.annotation 有值
  ast7,"after-turn-tree-nonempty",产品 TUI 完成至少一轮对话且消息已 persist,双 Esc 打开会话树,树节点数大于 0 且可见 user 正文
  ast8,"ctrl-t-hides-tools",产品树已打开且含 tool 行,"按 Ctrl+T 进入 no-tools","可见行无 kind=tool 且状态含 [no-tools]"
  ast8,"toggle-back-default","树在 no-tools",再按 Ctrl+T,"回到 default 谓词且状态无 [no-tools]"
  ast8,"labeled-only",树含带 annotation 与无 annotation 节点,"Ctrl+L 进入 labeled-only",仅 annotation 有值的行可见
  ast9,"fold-hides-descendants",树开且选中有子节点的可折行,按 Ctrl+Left（或 Alt+Left）,该节点 is_folded 且后代行不可见，帧含 ⊞ 或等价
  ast9,"unfold-restores",同上已折叠,按 Ctrl+Right（或 Alt+Right）,后代重新可见
  ast10,"fork-new-session",树开且选中节点,按 Shift+F,"fork+switch 后当前为 child、树关闭、父 JSONL 未改"
  ast10,"fork-user-before",树选中 kind=user,Shift+F,"Before：editor 含正文且 child 不含该 user id"
  ast10,"fork-non-user-at",树选中 assistant/tool,Shift+F,"At：child 含该节点 id 且 editor 未因 fork 填 user 正文"
  ast10,"fork-no-sibling-leak",父会话同层已有兄弟分支,对一侧路径 Shift+F,child 不含另一侧兄弟条目
  ast11,"open-shows-search-help",产品 TUI idle 且已打开会话树,渲染 Tree 槽一帧,帧内含 Search 或 Type to search 类提示，且含由键位解析得到的 move 或 filters 或 cycle 等 Help 片段
  ast11,"search-echo",产品会话树已打开,键入搜索串 foo,Search 行显示含 foo 的查询回显
  ast12,"label-persist",产品树已打开且选中节点,Shift+L 输入 keep 后 Enter,"树行含 [keep] 且 store 有对应该节点的 Label entry"
  ast13,"after-debug-tree-nonempty",已成功 /debug session-tree-multiturn,双 Esc 开树,树节点数大于 0 且可见 fixture user 正文
  ast14,"e2e-case-exists","","列出 tests/tui_e2e/pty.rs",存在产品会话树 #[ignore] 用例且注释或名含 session_tree
