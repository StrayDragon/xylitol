kind: llman.sdd.spec
name: "infra-bash"
purpose: "经 shell 子进程执行 Bash 命令，含流式输出与取消。"
valid_scope[2]: src/infra/bash_exec/,src/runtime_protocol/bash.rs
requirements[10]{req_id,title,statement}:
  be1,流式执行器,"System MUST 提供 XyBashExecutor，经 BashExecOpts 执行 shell 命令，携带可选 CancellationToken 与可选有界 mpsc chunk_tx 输出字节；chunk_tx 为 Some 时执行器 MUST 在该通道发送输出块（Full 时发送方合并）；为 None 时 MUST 静默累积且不要求回调。原 on_chunk 回调签名 MUST NOT 保留为公共端口。"
  be2,中止与取消,"BashExecutor MUST 支持取消，杀死整个进程组并将结果标为 cancelled。"
  be3,输出截断,"BashExecutor MUST 将输出截断到配置 max bytes，超限时将完整输出溢出到临时文件路径。"
  be4,会话条目,"SessionEntry MUST 支持 BashExecution 变体，携带 command、output、exit_code、cancelled、truncated、full_output_path、exclude_from_context。"
  be5,AgentSession API,"AgentSession MUST 暴露 execute_bash、record_bash_result、abort_bash，并将单/双 bang 前缀路由到执行器。"
  be6,上下文过滤,"构建 LLM 上下文时 System MUST 省略 exclude_from_context 为 true 的 BashExecution 条目。"
  b1,bash trait,"System MUST 在 BashOperations trait 后抽象 bash 执行，支持真实与 mock 实现供测试。"
  b2,bash hooks,"System MUST 支持 bash 执行的 pre-spawn 与 post-spawn hooks 以供扩展集成。"
  b3,超时逐级升级,"System MUST 实现逐级超时：先 SIGTERM，5 秒宽限后 SIGKILL。"
  be7,运行时可达 abort,"交互 bash 的取消入口 abort_bash MUST 可从 AgentRuntime::abort（&self）到达，无需独占 &mut AgentCapabilities 才能在 Driver::abort 路径杀进程树；CancellationToken 取消后 MUST 触发既有杀树路径（be2）。"
scenarios[12]{req_id,id,given,when,then}:
  be1,"stream-via-channel",chunk_tx 为 Some,命令打印多行,接收者在 execute 返回前收到覆盖输出的一个或多个字节块
  be1,"no-callback-port",审查 runtime_protocol bash 端口,查找 on_chunk 回调签名,公共 execute 仅接受 BashExecOpts 而无 FnMut on_chunk
  be2,abort,长时间命令执行中,调用 abort,进程组被杀且 cancelled 为 true
  be3,truncate,输出超过 max bytes,执行完成,truncated 为 true 且 full_output_path 已设置
  be4,entry,记录 bash 结果,append 到 session,条目存为 BashExecution 变体
  be5,"bang-include",用户输入以单 bang 开头,命令运行,结果记录 exclude_from_context false 并送 LLM
  be5,"bang-exclude",用户输入以双 bang 开头,命令运行,结果记录 exclude_from_context true 且从上下文省略
  be6,omit,history 有 exclude_from_context true 的 BashExecution 条目,构建 LLM 上下文,该条目被省略
  b1,mock,注册 mock BashOperations,调用 execute_bash,运行 mock 实现而非真实 shell
  b2,"pre-hook",已注册 pre-spawn hook,执行 bash 命令,spawn 前以命令字符串调用 hook
  b3,timeout,命令超过超时,以 timeout 调用 execute_bash,进程先收 SIGTERM 宽限后 SIGKILL
  be7,"runtime-abort-kills",BashExecHandler 持有进行中的 cancel token,"仅通过 &self abort_bash / AgentRuntime::abort 取消",进程组被杀且 cancelled 为 true
