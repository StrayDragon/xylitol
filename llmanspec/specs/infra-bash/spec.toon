kind: llman.sdd.spec
name: "infra-bash"
purpose: 经 shell 子进程执行 Bash 命令，含流式输出与取消。
valid_scope[2]: "infra 层 bash 执行","protocol 层"
requirements[11]{req_id,title,statement}:
  be1,流式执行器,System MUST 提供 XyBashExecutor，经 BashExecOpts 执行 shell 命令，携带可选 CancellationToken 与可选有界 mpsc chunk_tx 输出字节；chunk_tx 为 Some 时执行器 MUST 在该通道发送输出块（Full 时发送方合并）；为 None 时 MUST 静默累积且不要求回调。原 on_chunk 回调签名 MUST NOT 保留为公共端口。
  be2,中止与取消,BashExecutor MUST 支持取消，杀死整个进程组并将结果标为 cancelled。
  be3,输出截断,"BashExecutor MUST 将输出截断到配置 max bytes，超限时将完整输出溢出到临时文件；返回给调用方/会话的 output MUST 为截断尾部，并 MUST 追加 pi 形脚注 `[Full output: <path>. Truncated: <N> lines shown (<limit> limit)]`（path 不可用时用 `(unavailable)`）；未截断 MUST NOT 追加该脚注。"
  be4,会话条目,"新写入的 bang-bash MUST 持久化为 SessionEntry::Message（type=message），其 message 为 Env bashExecution（role=bashExecution），携带 command、output、exit_code、cancelled、truncated、full_output_path、exclude_from_context。MUST NOT 再对新写入使用顶层 type=bashExecution 变体。读路径 MUST NOT 将旧顶层 bashExecution/bash_execution 提升为合法上下文（按 agent-session-store s20 跳过该行并可观测 warn）。"
  be5,bash 执行入口,System MUST 经 Driver（或等价缝）暴露 bash 执行入口（execute_bash 或等价，含结果记录与中止）；单/双 bang 前缀 MUST 在产品面路由到执行器。
  be6,上下文过滤,"构建 LLM 上下文（含 ReAct 从 store 播种的 history）时 System MUST 省略 exclude_from_context 为 true 的 bashExecution（Message 内 nested）；单 bang 默认纳入。MUST NOT 依赖旧顶层 BashExecution 读提升。"
  b1,bash trait,System MUST 在 BashOperations trait 后抽象 bash 执行，支持真实与 mock 实现供测试。
  b2,bash hooks,"System MUST 支持 bash 执行的 pre-spawn 与 post-spawn hooks 以供扩展集成。"
  b3,超时逐级升级,当调用方提供有限超时时，System MUST 实现逐级超时：先 SIGTERM，5 秒宽限后 SIGKILL。未提供超时时 MUST NOT 仅因默认秒数触发该升级路径。
  be7,运行时可达 abort,"交互 bash 的取消入口 abort_bash MUST 可从 AgentRuntime::abort（&self）到达，无需独占 &mut AgentCapabilities 才能在 Driver::abort 路径杀进程树；CancellationToken 取消后 MUST 触发既有杀树路径（be2）。"
  be8,执行器超时可选,XyBashExecutor / BashExecOpts MUST 支持可选超时；省略时的默认策略由调用方决定——产品工具层 MUST 传入默认上限，执行器自身 MUST NOT 硬编码秒数；仍可经 CancellationToken 取消。
scenarios[14]{req_id,id,given,when,then,feature}:
  be8,"executor-omits-no-hardcode",直接构造未带超时的 BashExecOpts,执行短命令,执行器不因内置秒数杀进程且产品层传入的上限生效,false
  be1,stream-via-channel,chunk_tx 为 Some,命令打印多行,接收者在 execute 返回前收到覆盖输出的一个或多个字节块,false
  be1,no-callback-port,审查 protocol bash 端口,查找 on_chunk 回调签名,公共 execute 仅接受 BashExecOpts 而无 FnMut on_chunk,false
  be2,abort,长时间命令执行中,调用 abort,进程组被杀且 cancelled 为 true,false
  be3,truncate,输出超过 max bytes,执行完成,truncated 为 true 且 full_output_path 已设置；output 含 Full output 脚注与 lines shown,false
  be4,entry,记录 bash 结果,append 到 session,条目为 type=message 且 message.role=bashExecution,false
  be4,legacy-top-level-bash-read,session 文件含旧顶层 type=bashExecution,加载并构建上下文,该旧顶层行被跳过且上下文中无 bashExecution,false
  be5,bang-include,用户输入以单 bang 开头,命令运行,结果记录 exclude_from_context false 并送 LLM,false
  be5,bang-exclude,用户输入以双 bang 开头,命令运行,结果记录 exclude_from_context true 且从上下文省略,false
  be6,omit,history 有 exclude_from_context true 的 bashExecution（Message 内）,构建 LLM 上下文或 ReAct 播种,该条目被省略,false
  b1,mock,注册 mock BashOperations,调用 execute_bash,运行 mock 实现而非真实 shell,false
  b2,pre-hook,已注册 pre-spawn hook,执行 bash 命令,spawn 前以命令字符串调用 hook,false
  b3,timeout,命令超过超时,以 timeout 调用 execute_bash,进程先收 SIGTERM 宽限后 SIGKILL,false
  be7,runtime-abort-kills,BashExecHandler 持有进行中的 cancel token,"仅通过 &self abort_bash / AgentRuntime::abort 取消",进程组被杀且 cancelled 为 true,false
