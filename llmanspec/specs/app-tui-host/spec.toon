kind: llman.sdd.spec
name: "app-tui-host"
purpose: 产品 TUI 宿主：host 驱动引擎、终端生命周期、即时日志与尺寸降级。
valid_scope[1]: src/app/tui/
requirements[15]{req_id,title,statement}:
  meta0,"capability-shell","本 capability 由变更 c450-revise-app-tui-contract 引入；归档合并后以 delta 增补的 requirements 为准。本壳 MUST 保持非空以满足 BDD 护栏。"
  ath1,"host-driven-engine","产品 TUI MUST 以 host 驱动 packages/xylitol-tui：使用 dispatch_event、request_render、try_render、idle_tick；产品路径 MUST NOT 调用 TUI::start()。"
  ath2,"terminal-restore",产品 TUI MUST 在正常退出、错误退出与 panic 路径恢复终端（关闭 raw mode 等），不得把用户终端留在损坏状态；MUST 安装 panic hook（仅靠 Drop 不足）。
  ath3,"infra-logging-default","debug 构建下产品 TUI MUST 默认启用即时文件日志（人类与 agent 可用 tail -f 观察）；release 构建 MUST 默认关闭，仍可通过 XYLITOL_DEBUG 或 RUST_LOG 显式打开。src/app/tui/AGENTS.md MUST 写明日志路径与查看方式。"
  ath4,"resize-and-min-size",终端 resize MUST 触发重绘且不得丢失输入焦点语义；当引擎因极端尺寸无法安全渲染时 MUST 干净恢复终端并退出进程，MUST NOT 卡死终端模拟器。宽度小于 40 或高度小于 6 时 MUST 显示友好提示而非 panic。
  ath5,"host-harness-testable","产品 TUI host MUST 将事件推进与终端 I/O 解耦，使 min-size 提示、dispatch_event 与无 TUI::start 路径可在无真 TTY 的 harness（注入 HostEvent / TestTerminal）中验证。"
  ath6,"shared-effect-pump",产品 TUI MUST 经单一 drain_pending（或等价共享入口）消费 HostSession pending 并调用 Driver/dispatch；合成 harness 泵 MUST 复用同一入口或与其同序同分支，MUST NOT 维护与生产循环分叉的第二份 slash/bash/steer 副作用 match。
  ath7,"single-mux-loop","产品 TUI host MUST 以单一扇入模型同时等待终端输入、idle tick、可选 agent EventStream、可选 bang 完成与 bang chunk：生产 run_host_loop 与 harness MUST 调用同一共享 bang/主环事件臂（或单一 select 拓扑）；MUST NOT 保留与共享入口分叉的第三套 bang Esc select；bang 进行中 Esc MUST 仍可达 Driver::abort 并走 note_bash_cancelled；chunk 到达 MUST 仅标记 dirty 并在 Tick 或 BangDone 时 try_render，MUST NOT 每字节强制全屏重绘；进行中的 bang MUST 仍可 poll agent EventStream（不得 bang-only 饿死 agent）。"
  ath8,"abort-drops-stream-events","产品 TUI 在用户 Busy Esc 置 pending_abort 的同一同步步进内 MUST 立即臂装 suppress_xy_until_stream_end（或等价门闩），使在随后 drain_pending 调用 note_user_abort / Driver::abort 之前到达的 ThinkingDelta / TextDelta / 会提交正文的 AgentEnd MUST NOT 经 apply_xy_event 复活 busy 或写出 assistant 正文；note_user_abort 仍 MUST 清空 streaming_thinking 与 streaming_assistant；Driver::abort MUST 仍被调用；下一轮 submit MUST 可正常开始。"
  ath10,"agents-layout-map","src/app/tui/AGENTS.md MUST 提供 kimi 式本地布局地图：目录/文件职责一句话、协调者（host/mod）与可下沉模块（effects/bridge/layout/widgets/commands）边界、硬约束指针（Esc 归属与 bang/agent abort 分岔、禁止 reach-in、禁止继续堆 God 文件）、验证命令指针；MUST NOT 把进度板或易腐清单写入 AGENTS；MUST 保留对 PI_DELTAS 与 write-tui skill 的指针。"
  ath9,"qa-baseline-before-refactor","在修改 abort 抑制时序、合并 bang select、或拆分 HostSession 模块之前，产品 TUI MUST 先落地可复现的 QA 基线：harness 与生产 bang Esc 路径 MUST 经同一共享 helper（或同序同分支等价入口）可测；MUST 提供 rstest-bdd feature 覆盖 abort/bang Esc/overlay Esc/队列键位；既有 ABS/HRS/BASE harness 与相关 PTY bang Esc 测 MUST 保持绿灯。本要求不豁免 ath6/ath7/ath8。"
  ath11,"abort-suppress-before-drain","Busy Esc（非 overlay）MUST 在 HostSession::step 内同步臂装 Xy 抑制，MUST NOT 仅依赖下一轮循环顶部的 drain_pending 才开始丢弃该轮流事件；bang Esc 路径 MUST 继续走 note_bash_cancelled（无 suppress_xy），不得与 agent abort 文案混用。"
  ath12,"host-module-boundaries",产品 TUI MUST 将 HostSession 协调逻辑与可单测切片分离：pending 副作用标志 MUST 收拢为 PendingOps（或等价单一结构）；输入策略、session tree mount、models mount MUST 可位于 host 子模块或独立文件；UiRoot MUST 将 tree / models / editor 路由臂拆到可导航单元；组件与 layout MUST NOT 直接调用 Driver；单源文件逼近约 1200 行时 MUST 优先拆分而非继续堆叠。
  ath13,"session-read-errors-surfaced","产品 TUI 在 travel/fork/label 等路径调用 Driver::get_messages（或等价）失败时 MUST 向用户展示 system/error note，MUST NOT 以 unwrap_or_default 静默得到空 transcript 并当作成功。"
  ath14,"session-list-seam","产品 TUI 获取可 resume 会话列表时 MUST 经 Driver（或 app::core 公开 seam）list_sessions 或等价 API，MUST NOT 从 app/tui 直接 import infra::session / 直接读 sessions 目录；switch 成功后 MUST 重建 transcript 并清除与旧 session 绑定的树槽/pending UI 态。"
scenarios[20]{req_id,id,given,when,then}:
  meta0,"shell-present","","列出 llmanspec/specs/app-tui-host",spec.toon 存在且 purpose 为中文
  ath1,"no-start","",审计产品 TUI 源码,"无 TUI::start；存在 host 驱动调用"
  ath2,"panic-restore","",TUI 运行中发生 panic,panic hook 恢复终端后用户 shell 可用
  ath3,"debug-tail","",以 debug 构建启动 TUI,日志文件持续写入且 AGENTS 记载路径
  ath4,"tiny-terminal","",终端高度为 3,显示放大提示或干净退出，进程不挂死
  ath5,"harness-min-size",TestTerminal 尺寸为 20x3,HostSession 推进一帧,视口含放大提示且进程逻辑不 panic
  ath5,"harness-no-start",产品 TUI 源码与 host 单元测试,审计调用点,"产品路径无 TUI::start 调用"
  ath6,"one-pump",生产 host loop 与 harness 泵均接线,审查 drain_pending 调用点,两侧共用同一函数且无重复的 PendingSlash match 副本
  ath7,"one-select",审查 run_host_loop 与 harness bang 路径,存在进行中的 bang,两侧共用同一 helper/扇入；无第三份手写 bang Esc select
  ath7,"esc-during-stream",hanging bang 已 uplink 且有 chunk,按 Esc,"Driver::abort 被调用且块呈 cancelled"
  ath8,"abort-then-deltas-ignored",busy 已 Esc abort,再泵入 ThinkingDelta 与 TextDelta 与 AgentEnd,无 assistant 正文条目且 status idle
  ath8,"esc-xy-before-drain",agent busy,Esc 后在 drain_pending 前注入 TextDelta,该 delta 被丢弃且 UI 不回 busy
  ath10,"layout-section-present",打开 src/app/tui/AGENTS.md,审计文档结构,含文件布局表、模块职责、硬约束与验证指针，且无进度表
  ath9,"shared-bang-helper",生产 mod.rs bang 环与 harness bang Esc 测并存,审查 bang Esc 消费路径,两侧调用同一共享 helper 或同序同分支入口，无第三份手写 select
  ath9,"bdd-features-present","tests/features 含 app-tui abort/bang/esc/queue feature","cargo test --test bdd -- --test-threads=1","新增 app-tui 场景全部通过且既有核心 BDD 不回归"
  ath11,"bang-abort-unchanged",hanging bang 中 Esc,取消,块呈 cancelled 且无 System Aborted 混用
  ath12,"pending-ops-bag",审查 HostSession 字段,重构后,pending_* 位于 PendingOps（或等价）且 drain_pending 仍为唯一异步泵
  ath12,"no-driver-in-widgets",审计 layout/widgets,调用点,无 Driver 直接调用
  ath13,"get-messages-error-note",Driver get_messages 返回 Err,执行 travel,scrollback 含失败提示且不假装空树成功
  ath14,"list-via-driver",产品 harness 或单元,调用 list 路径,经 Driver/seam 返回且无 tui→infra::session 依赖
