kind: llman.sdd.spec
name: "agent-runtime"
purpose: "薄编排 ReAct 运行时：AgentRuntime 循环、工具批执行、steer/follow-up 队列、abort 与 XyEvent 流（不含 adk / OutputGuard）。"
valid_scope[3]: src/agent/,src/runtime_protocol/,tests/
requirements[20]{req_id,title,statement}:
  ar1,"thin-react-loop",agent/ 层 MUST 以 AgentRuntime（runtime/react.rs）驱动 ReAct：调用 XyModel、执行工具、直至无 tool_calls 或达 max_iterations；MUST NOT 在循环文件内联 bash 实现或厂商 HTTP。
  ar2,"xy-event-stream",运行时 MUST 以 XyEvent 异步流对外输出（TextDelta / ThinkingDelta / Tool* / Turn* / QueueUpdate / Error 等）；MUST NOT 再引入 AgentEvent 或 adk 事件类型。
  ar3,"tool-batch-before-next-model",同一模型响应中的全部工具调用 MUST 在下一轮模型调用前执行完毕（可并行或串行）；MUST NOT 因流结束 Done 而提前结束含 tool_calls 的回合。
  ar4,"builder-ports",Agent 装配 MUST 经 AgentBuilder，依赖 runtime_protocol 端口与纯数据；app 组合根注入具体 infra 实现。
  ar5,"sessionstore-eventsink",runtime_protocol MUST 提供 SessionStore 与 EventSink；SessionManager / EventBus 为实现。
  ar6,"mutable-slots-next-turn",set_tools / set_hooks / set_system_prompt 等 MUST 只影响下一轮 run；进行中回合使用启动快照。
  ar7,"hooks-at-tool-boundary",工具执行前 MUST 跑 before 链（可拒绝）；之后 MUST 跑 after 链；无 hook 时零开销。
  ar8,"pending-queues",MUST 提供独立的 steer 与 follow_up 队列（enqueue/drain/clear）；ReAct MUST 在模型调用前 drain steer，在将结束前 drain follow_up（非空则继续）。
  ar9,"queue-update-events","入队/drain/clear 后 MUST 发射 XyEvent::QueueUpdate（steer_count / follow_up_count）。"
  ar10,"abort-queue-semantics",abort MUST 清空 steer、保留 follow_up；MUST 取消当前 run CancellationToken 与进行中工具/交互 bash；MUST NOT 仅靠无人订阅的 EventBus 旁路传队列状态。
  ar11,"abort-resets-token",每次 run 开始 MUST 安装新 CancellationToken；abort 后下一次 run MUST NOT 因沿用已取消 token 立刻 aborted。
  ar12,"abort-cancels-provider-stream","模型流进行中 abort MUST 停止 poll 并 drop provider stream，以 aborted 结束；应用面 MUST 经 Driver::abort。"
  ar13,"no-rloop-name","src/ MUST NOT 使用 r#loop 标识符；循环模块名为 runtime::react。"
  ar14,"facade-removed","MUST NOT 恢复 agent::facade 中枢；AgentRuntime / AgentCapabilities 由 agent/mod 再导出；app 禁止 reach agent::runtime / agent::session 内部。"
  ar15,"unknown-event-degrade",应用面/bridge 对未识别 XyEvent 变体 MUST 降级（日志），MUST NOT panic。
  ar16,defaults,未显式配置时 MUST 有可测默认：thinking level、max_iterations、compaction 阈值等集中定义。
  ar17,"cwd-validate",创建/导入会话时 MUST 校验工作目录可访问，失败返回含路径的错误。
  ar18,"wire-event-mapping","MUST 提供 XyEvent ↔ protocol::Event 映射（From/TryFrom 或等价），覆盖闭集内变体。"
  ar19,Driver context 重载缝,InProcessDriver（经 app/core 助手）MUST 能在 Trust 语义下重载磁盘 context/SYSTEM/APPEND 并应用到 agent；未信任 MUST NOT 注入项目侧 context；重载 MUST NOT 清空 transcript。
  ar20,Driver skills 重载缝,InProcessDriver（经 app/core 助手）MUST 能在 Trust 语义下重载磁盘 skills 并应用到 agent；MUST 能查询当前已加载 skill 名（供后续 $ 展开）；未信任 MUST NOT 注入项目 skills；重载 MUST NOT 清空 transcript；本要求 MUST NOT 依赖 /session 或 /status skills UI。
scenarios[25]{req_id,id,given,when,then}:
  ar1,"react-terminates",mock 模型先 tool 后无 tool,运行 AgentRuntime,先执行工具再结束且无 adk 类型
  ar2,"stream-is-xyevent",消费事件流,轮询,每项为 XyEvent
  ar3,"continues-after-tools",模型返回 FunctionCall 后 Done,循环,继续下一轮模型调用并带上 toolResult
  ar3,"done-not-turn-end",流含 tool 与 Done,观察循环,MUST NOT 仅因 Done 结束回合
  ar8,"steer-before-model",steer 队列非空,即将模型调用,历史含 steer 消息
  ar8,"followup-extends",无 tool 且 follow_up 非空,将结束时,继续循环而非 AgentEnd
  ar9,"queue-update",enqueue steer,观察流,出现 QueueUpdate 且计数正确
  ar10,"abort-clears-steer",有 steer 与 follow_up,abort,steer 空且 follow_up 保留
  ar11,"second-run-after-abort",abort 后,再次 run,正常完成而非立即 aborted
  ar12,"abort-drops-sse",模型流挂起,"Driver::abort",流停止且回合 aborted
  ar13,"no-rloop",在 src 中搜索 r#loop 标识符,运行检查,零匹配
  ar14,"no-facade",检查 agent/facade.rs,不存在该模块,facade 保持移除
  ar15,"unknown-ok",bridge 收到未知变体,处理,不 panic
  ar16,"default-thinking",未设 thinking,查询,返回默认 medium 或配置约定值
  ar17,"cwd-missing",cwd 不存在,创建会话,错误含路径
  ar18,roundtrip,XyEvent TextDelta,经 protocol 往返,字段保留
  ar7,"before-denies",before hook 拒绝 bash,循环到 bash,"不执行并回写 tool-error"
  ar6,"mid-turn-set-tools",回合进行中 set_tools,本回合,仍用旧工具集
  ar4,"builder-build",AgentBuilder 装配,build,得到 AgentRuntime
  ar5,"ports-exist",检查 runtime_protocol,SessionStore 与 EventSink,trait 存在且可被实现
  ar10,"abort-cancels-bang",交互 bang 进行中,abort,子进程 cancelled
  ar19,"trusted-reload",项目信任且 cwd 有 AGENTS.md,调用 reload_prompt_context,agent 系统提示含该 AGENTS 内容
  ar19,"untrusted-skip",项目未信任且仅项目目录有 AGENTS.md,调用 reload_prompt_context,系统提示 MUST NOT 含该项目 AGENTS 正文
  ar20,"trusted-reload",项目信任且 cwd 有 skill,调用 reload_skills,agent 系统提示或目录 API 含该 skill 名
  ar20,"untrusted-skip",项目未信任且仅项目目录有 skill,调用 reload_skills,目录与系统提示 MUST NOT 含该项目 skill
