kind: llman.sdd.spec
name: "app-tui-commands"
purpose: "斜杠命令与 protocol::Command / dispatch 映射。"
valid_scope[1]: src/app/tui/
requirements[12]{req_id,title,statement}:
  meta0,"capability-shell","本 capability 由变更 c450-revise-app-tui-contract 引入；归档合并后以 delta 增补的 requirements 为准。本壳 MUST 保持非空以满足 BDD 护栏。"
  atm1,"slash-exit-model",TUI MUST 解析斜杠命令 /exit（映射 Quit）与 /model：无参 MUST 打开替换 editor 槽的 fuzzy 模型列表（GetAvailableModels + 选定后 SetModel）；有参 /model <id> MUST 直接 SetModel；键入 /model 参数时 MAY/MUST 经包 SlashArgCompletionSource 内联补全（见 ati23），补全写入后仍走有参 SetModel；MUST NOT 再把无参 /model 做成 CycleModel 静默轮换；MUST NOT 引入 /models 主路径；未知斜杠 MUST 显示内联错误且 MUST NOT 崩溃。
  atm2,"dispatch-shared","除 Prompt、Quit 与传输专用命令外，斜杠执行 MUST 走 app::core::dispatch 共享路径，MUST NOT 发明平行命令词表。"
  atm3,"extensible-commands",命令面 MUST 允许后续追加斜杠（如 /compact /help）而不改引擎；MVP 仅要求 /exit 与 /model。
  atm4,"idle-slash-submit",产品 idle Enter 提交以 / 开头的文本时 MUST 解析 /exit 与 /model（无参开列表槽；有参直选）；未知斜杠 MUST 写入系统错误行且 MUST NOT 崩溃或退出。
  atm5,"debug-scene-slash","产品 TUI 在 debug 构建 idle 时 MUST 解析斜杠 /debug 与 /debug <scene>（空格参数；MUST NOT 要求冒号形式）：无 scene 时 MUST 以系统提示列出可用场景及描述且 MUST NOT 改当前 session；已知 scene（至少 session-tree-multiturn、session-tree-labeled）MUST 经 Driver::load_debug_scene 新建隔离 debug-* session、写入 fixture 后 switch 并重建 transcript；未知 scene MUST 提示错误且 MUST NOT 崩溃；debug 构建 MUST 为 /debug 提供与 /model 同类的参数补全（SlashArgCompletionSource）；MUST NOT 把 debug 场景写成 protocol::Command 主路径；MUST NOT 覆写用户装载前的当前 session 文件内容。"
  atm6,"slash-session-tree-fork","产品 TUI idle 时 MUST 解析 /session-tree 与 /session-fork：/session-tree MUST 打开 MessageHistory 会话树（与双 Esc 同路径）；/session-fork MUST 对当前 session leaf 执行与树内 Shift+F 相同的产品 fork（user→Before、非 user→At）并 switch 到 child；busy 时二者 MUST 拒绝并提示且 MUST NOT 开树或 fork；SlashCommandSource MUST 列出 session-tree 与 session-fork；MUST NOT 再将 /tree 或 /fork 识别为上述命令；MUST NOT 发明平行 fork 语义（含 pi 式 user 消息选择器）。"
  atm7,"commands-module-growth","产品 TUI slash/bang 解析 MUST 继续收口于 commands 模块（可多文件），执行仍经 pending + drain_pending → protocol::Command / dispatch 或 Driver；MUST NOT 在 UiRoot/widgets 内散落第二套 slash 语义。"
  atm8,"slash-session-io","产品 TUI idle 时 MUST 解析 /session-compact、/session-export 与 /session-import：/session-compact 无参 MUST 经 dispatch 调用 Compact 并以系统行提示是否压缩；带非空参数 MUST 短错误 usage 且 MUST NOT 调用 Compact；/session-export 可选 path MUST 默认导出 HTML（对齐 pi；无参时可用 Driver/dispatch 默认 export.html），path 以 .jsonl 结尾 MUST 走 ExportJsonl，否则 ExportHtml，成功 MUST 回报写入路径；/session-import 缺 path MUST usage 错误，有 path MUST 先以 editor 槽 Yes/No 确认再 ImportJsonl 并 switch/重建 transcript，取消 MUST NOT 导入；三者 busy 时 MUST 拒绝；SlashCommandSource MUST 列出三者；旧名 /compact /export /import MUST NOT 被识别；MUST 走 app::core::dispatch，MUST NOT 发明平行词表。"
  atm9,"slash-session-info","产品 TUI idle 提交无参 /session 时 MUST 经 dispatch 调用 GetSessionStats（及必要 GetState）并将会话信息与统计以 scrollback/系统文本块展示（对齐 pi：信息转储而非操作菜单）；带参数 MUST 短错误或不识别为本命令；busy 时 MUST 拒绝；SlashCommandSource MUST 列出 session；MUST NOT 实现 /session 子命令操作板。"
  atm10,"slash-session-resume","产品 TUI idle 提交无参 /session-resume 时 MUST 打开替换 editor 槽的会话 SelectList（数据经 Driver list_sessions 或等价 seam，mtime 降序，行含 name（若有）与 id）；Enter 选定 MUST SwitchSession 并重建 transcript、关闭其它槽；Esc 取消 MUST NOT 切换；busy 时 MUST 拒绝；旧名 /resume MUST NOT 被识别；MVP MUST NOT 要求 /session-resume 路径直参；TUI MUST NOT reach infra::session。"
  atm11,"slash-session-lifecycle","产品 TUI idle 时 MUST 解析 /session-new、/session-clone 与 /session-name：/session-new 无参 MUST 经 Driver::new_session（或等价）创建空会话并切换、清空 transcript，带参 MUST usage 错误；/session-clone 无参 MUST 对当前 leaf 以 ForkPosition::At 执行 fork_session 并 switch（对齐 pi /clone；MUST NOT 套用 /session-fork 的 Before/At 规则），无 leaf MUST 提示且 MUST NOT fork，带参 MUST usage 错误；/session-name 无参 MUST 显示当前会话名或 usage，有参 MUST 经 Driver::set_session_name（换行→空格 trim）写入并回报最终名，规范化与输入不同时 MUST 提示；三者 busy 时 MUST 拒绝；SlashCommandSource MUST 列出三者；旧名 /new /clone /name MUST NOT 被识别；MUST NOT 发明平行词表或 pi 式 user 选择器。"
scenarios[28]{req_id,id,given,when,then}:
  meta0,"shell-present","","列出 llmanspec/specs/app-tui-commands",spec.toon 存在且 purpose 为中文
  atm1,"model-opens-picker",idle 且会话树关闭,Enter 提交无参 /model,打开 models 槽且不调用 CycleModel
  atm1,"model-id-after-completion",idle 编辑器经补全得到 /model fake,Enter 提交,SetModel 或等价被调用
  atm2,"model-via-dispatch","",用户输入 /model,经 dispatch 或等价 Driver 方法切换模型
  atm3,"unknown-slash","","用户输入 /not-a-command",显示内联错误且会话继续
  atm4,"model-bare-not-cycle",产品 harness idle,/model Enter,pending 为开 picker 而非 CycleModel
  atm5,"list-scenes",产品 idle,提交 /debug,"系统提示含 session-tree-multiturn 或 session-tree-labeled 及描述"
  atm5,"load-multiturn",产品 idle,"提交 /debug session-tree-multiturn","当前 session 变为 debug- 前缀且 transcript/树可含 fixture user 正文"
  atm5,"arg-completion",产品 debug 构建 idle 且已键入 /debug 加空格,补全 popup 出现场景 id,可 Tab/Enter 写入 editor
  atm6,"slash-session-tree-opens",产品 idle,提交 /session-tree,"EditorSlot::Tree 打开且含 Search 或 Type to search"
  atm6,"slash-session-fork-leaf",产品 idle 且 session 有 leaf,提交 /session-fork,fork+switch 发生或 ScriptedDriver fork 被调用
  atm6,"old-tree-fork-unknown",产品 idle,"提交 /tree 或 /fork","显示 unknown command 类系统错误且不开树不 fork"
  atm7,"parse-stays-in-commands",新增或拆分 slash 解析,审计调用点,解析定义在 commands 下且 layout 无独立 parse 副本
  atm8,"compact-bare",产品 idle,提交 /session-compact,Compact 经 dispatch 被调用或 ScriptedDriver compact 被调用
  atm8,"compact-rejects-args",产品 idle,提交 /session-compact please,出现 usage 类错误且未 compact
  atm8,"export-default-html",产品 idle,提交无参 /session-export,走 ExportHtml 或等价且系统行含导出路径
  atm8,"export-jsonl-suffix",产品 idle,提交 /session-export /tmp/out.jsonl,走 ExportJsonl 或等价
  atm8,"import-confirm-cancel",产品 idle,提交 /session-import /tmp/a.jsonl 后在确认槽选 No 或 Esc,未 import 且有取消提示
  atm9,"session-dumps-stats",产品 idle,提交 /session,scrollback 或系统行含 session id 或消息统计字段
  atm9,"session-rejects-args",产品 idle,提交 /session info,出现错误或不识别为 dump 成功路径
  atm10,"resume-opens-list",产品 idle,提交 /session-resume,editor 槽出现会话 SelectList
  atm10,"resume-switch",产品 idle 且列表有项,在 resume 槽 Enter 选定,SwitchSession 发生且 transcript 刷新
  atm11,"session-new-bare",产品 idle,提交 /session-new,Driver new_session 被调用且 transcript 清空或系统行提示新会话
  atm11,"session-new-rejects-args",产品 idle,提交 /session-new x,出现 usage 类错误且未 new_session
  atm11,"session-clone-at-leaf",产品 idle 且有 leaf,提交 /session-clone,"fork_session 以 At 调用并 switch"
  atm11,"session-clone-no-leaf",产品 idle 且无 leaf,提交 /session-clone,提示无 leaf 且未 fork
  atm11,"session-name-set",产品 idle,提交 /session-name hello,set_session_name 被调用或系统行含最终名
  atm11,"old-lifecycle-unknown",产品 idle,"提交 /new 或 /clone 或 /name","显示 unknown command 类错误"
