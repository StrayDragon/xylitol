kind: llman.sdd.spec
name: "app-tui-commands"
purpose: "斜杠命令与 protocol::Command / dispatch 映射。"
valid_scope[1]: src/app/tui/
requirements[7]{req_id,title,statement}:
  meta0,"capability-shell","本 capability 由变更 c450-revise-app-tui-contract 引入；归档合并后以 delta 增补的 requirements 为准。本壳 MUST 保持非空以满足 BDD 护栏。"
  atm1,"slash-exit-model",TUI MUST 解析斜杠命令 /exit（映射 Quit）与 /model：无参 MUST 打开替换 editor 槽的 fuzzy 模型列表（GetAvailableModels + 选定后 SetModel）；有参 /model <id> MUST 直接 SetModel；键入 /model 参数时 MAY/MUST 经包 SlashArgCompletionSource 内联补全（见 ati23），补全写入后仍走有参 SetModel；MUST NOT 再把无参 /model 做成 CycleModel 静默轮换；MUST NOT 引入 /models 主路径；未知斜杠 MUST 显示内联错误且 MUST NOT 崩溃。
  atm2,"dispatch-shared","除 Prompt、Quit 与传输专用命令外，斜杠执行 MUST 走 app::core::dispatch 共享路径，MUST NOT 发明平行命令词表。"
  atm3,"extensible-commands",命令面 MUST 允许后续追加斜杠（如 /compact /help）而不改引擎；MVP 仅要求 /exit 与 /model。
  atm4,"idle-slash-submit",产品 idle Enter 提交以 / 开头的文本时 MUST 解析 /exit 与 /model（无参开列表槽；有参直选）；未知斜杠 MUST 写入系统错误行且 MUST NOT 崩溃或退出。
  atm5,"debug-scene-slash","产品 TUI 在 debug 构建 idle 时 MUST 解析斜杠 /debug 与 /debug <scene>（空格参数；MUST NOT 要求冒号形式）：无 scene 时 MUST 以系统提示列出可用场景及描述且 MUST NOT 改当前 session；已知 scene（至少 session-tree-multiturn、session-tree-labeled）MUST 经 Driver::load_debug_scene 新建隔离 debug-* session、写入 fixture 后 switch 并重建 transcript；未知 scene MUST 提示错误且 MUST NOT 崩溃；debug 构建 MUST 为 /debug 提供与 /model 同类的参数补全（SlashArgCompletionSource）；MUST NOT 把 debug 场景写成 protocol::Command 主路径；MUST NOT 覆写用户装载前的当前 session 文件内容。"
  atm6,"slash-tree-fork","产品 TUI idle 时 MUST 解析 /tree 与 /fork：/tree MUST 打开 MessageHistory 会话树（与双 Esc 同路径）；/fork MUST 对当前 session leaf 执行与树内 Shift+F 相同的产品 fork（user→Before、非 user→At）并 switch 到 child；busy 时二者 MUST 拒绝并提示且 MUST NOT 开树或 fork；SlashCommandSource MUST 列出 tree 与 fork；MUST NOT 发明平行 fork 语义。"
scenarios[11]{req_id,id,given,when,then}:
  meta0,"shell-present","","列出 llmanspec/specs/app-tui-commands",spec.toon 存在且 purpose 为中文
  atm1,"model-opens-picker",idle 且会话树关闭,Enter 提交无参 /model,打开 models 槽且不调用 CycleModel
  atm1,"model-id-after-completion",idle 编辑器经补全得到 /model fake,Enter 提交,SetModel 或等价被调用
  atm2,"model-via-dispatch","",用户输入 /model,经 dispatch 或等价 Driver 方法切换模型
  atm3,"unknown-slash","","用户输入 /not-a-command",显示内联错误且会话继续
  atm4,"model-bare-not-cycle",产品 harness idle,/model Enter,pending 为开 picker 而非 CycleModel
  atm5,"list-scenes",产品 idle,提交 /debug,系统提示含 session-tree-multiturn 或 session-tree-labeled 及描述
  atm5,"load-multiturn",产品 idle,提交 /debug session-tree-multiturn,当前 session 变为 debug- 前缀且 transcript/树可含 fixture user 正文
  atm5,"arg-completion",产品 debug 构建 idle 且已键入 /debug 加空格,补全 popup 出现场景 id,可 Tab/Enter 写入 editor
  atm6,"slash-tree-opens",产品 idle,提交 /tree,EditorSlot::Tree 打开且含 Search 或 Type to search
  atm6,"slash-fork-leaf",产品 idle 且 session 有 leaf,提交 /fork,fork+switch 发生或 ScriptedDriver fork 被调用
