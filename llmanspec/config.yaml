# yaml-language-server: $schema=https://raw.githubusercontent.com/StrayDragon/llman/main/artifacts/schema/configs/en/llmanspec-config.schema.json
schema: spec-driven
locale: zh-Hans
extra_skills:
- llman-sdd-ff
- llman-sdd-arch-review
- llman-sdd-wayfinder
- llman-sdd-research
bdd:
  framework: rstest-bdd
  feature_dir: tests/features
  default_language: zh-CN
  run_command: cargo test --test bdd
  # Bound-scenario sources for `llman sdd list --specs` harness split
  # (harness-bound / harness-unbound); see llman change
  # harness-bound-declarable-split.
  bindings:
    - kind: scenario-attrs
      files: ["tests/bdd/bindings_*.rs"]
  verify_prompt: Each scenario in the feature file MUST be mapped to an implemented step definition. Run `cargo test --test bdd` to confirm all scenarios pass.
