---
description: Token-efficient provider-trace / log inspection — never ingest whole JSONL
alwaysApply: true
---

# Provider trace & log：token 效率

诊断「thinking vs text / 上游 vs 映射」时，**禁止**把 `provider-trace.jsonl` 或 `xylitol.log` 整文件读进上下文。

## 路径

- `~/.xylitol/logs/provider-trace.jsonl`（schema `xylitol.provider_trace.v1`）
- `~/.xylitol/logs/xylitol.log`

## 必用命令（先窄后宽）

```bash
# 最近 N 行
tail -n 40 ~/.xylitol/logs/provider-trace.jsonl

# 只看 kinds / variants 计数
python3 -c "
import json
from collections import Counter
c,v=Counter(),Counter()
for line in open('$HOME/.xylitol/logs/provider-trace.jsonl'):
  if not line.strip(): continue
  o=json.loads(line); c[o.get('kind')]+=1
  if o.get('variant'): v[o['variant']]+=1
print(dict(c)); print(dict(v))
"

# 按 request_id 抽一行对照（先 rg 拿 id）
rg -n '"request_id"' ~/.xylitol/logs/provider-trace.jsonl | tail -3
rg 'response\.(reasoning_text|output_text)\.delta|"variant":"ThinkingDelta"|"variant":"TextDelta"' \
  ~/.xylitol/logs/provider-trace.jsonl | tail -20
```

## 硬约束

1. **MUST** 用 `rg` / `tail` / 小段 `python -c`；**MUST NOT** `Read` 整个 JSONL/log。
2. 单次工具输出控制在几十行；需要更多再分页（`tail`/`rg` + `offset`）。
3. 对照结论只引用：同一 `request_id` 下 `kind=raw` 的 `event` vs `kind=mapped` 的 `variant`。
4. SSOT 形状见 `llmanspec/changes/archive/2026-07-15-c1000-add-infra-provider-trace/design.md`（勿整篇粘贴进回复）。
