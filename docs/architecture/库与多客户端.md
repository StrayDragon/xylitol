# 库与多客户端

> 用户/产品视角：同一内核如何被多种 client 与嵌入方共用。不写 API 签名与模块路径。
> 现状对齐：2026-07-18。

## 用户怎么碰到

- **TUI**：本地交互终端面（**CLI 默认入口**；走统一 Driver + 统一事件流）。
- **Print**：本地一次性对话，开箱主路径之一。
- **Server**：远程托管同一内核，经线协议对外；需启用对应能力。
- **远程薄端**：理想上连 Server 的独立客户端——**线协议已通，客户端产品面尚未接线**。
- **嵌入库**：外部程序把 xylitol 当库，自建界面。

用户改道（插话 / 续跑 / 中止）只经统一的「驱动」入口，不直达对话循环内部。

## 一条主线（产品）

```text
装配 → 驱动（进程内或远程）→ 对话循环 → 统一事件流 → 该面渲染
```

```mermaid
flowchart LR
  Clients["Print / TUI / Server / 嵌入"] --> Driver["统一驱动"]
  Driver --> Loop["对话循环"]
  Loop --> Events["统一事件流"]
  Events --> Render["各面各自渲染"]
```

## Client 矩阵

| Client | 角色 | 今日状态 |
|---|---|---|
| **TUI** | 本地交互（默认） | ✅ 已开闸并消费 Driver 事件流 |
| **Print** | 本地一次性对话 | ✅ 已走主线 |
| **Server** | 远程托管同一内核 | ✅ 已挂到统一驱动（配置/feature 启用） |
| **远程薄端** | 连 Server 的独立客户端 | 🔴 预留：线协议/队列可上线；无已交付远程客户端应用 |
| **嵌入库** | 外部自建面 | ✅ 公开装配缝 + 精选契约 |
| **GUI / Web（未来）** | 桌面或浏览器控制台 | 🔴 未开 |

## 两层对外契约（产品规则）

| 层 | 给谁 | 写什么 |
|---|---|---|
| **库契约** | 替换端口、跨面事件、配置元数据 | 精选稳定符号（见 `src/AGENTS.md`） |
| **应用缝** | 多 client / 嵌入方 | 装配、驱动、命令分发、（可选）线协议 |

应用缝不必套品牌前缀；需要新能力时扩缝，不要伸手进内部实现。

## 嵌入方产品规则

1. **优先**公开装配缝 + 精选库契约。
2. **不要**把内部实现类型当稳定 API。
3. 未配置的能力（如 MCP）必须 **zero-cost**。
4. 已知泄漏与非承诺项 → 一行见 `src/embed.rs` 文档注释；不在此展开。

## 理想 vs 现状

| | 状态 |
|---|---|
| Print / Server / 嵌入走统一驱动 | ✅ |
| TUI 消费同一事件流（会话树、steer、slash、bang…） | ✅ |
| CLI 默认进 TUI | ✅ |
| 线协议部分生命周期仍可降级 | 🟢 远程展示打磨中 |
| 独立远程薄端应用 | 🔴 未接线 |

## 与其它文档

- [产品分层总览.md](./产品分层总览.md)
- [一轮对话.md](./一轮对话.md)
- [用户可见事件.md](./用户可见事件.md)
- [插话续跑与中止.md](./插话续跑与中止.md)
- [远程体验与线协议.md](./远程体验与线协议.md)
- 代码边界 SSOT：`src/AGENTS.md`
