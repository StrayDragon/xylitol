# 术语表（黑话 ↔ 标准词）

> 文档召回锚点：agent / 新人按自然语言检索时，凭本表把内部黑话映射到可搜的标准词与英文。
> 用法：新写作黑话首现须附本表标准词或英文对照（根 `AGENTS.md` 工作原则）；编号引用见 [变更索引.md](./变更索引.md)。
> 现状对齐：2026-08-24。

## A. SDD 流程词

| 黑话 | 标准词 | EN | 定义 | 状态 |
|---|---|---|---|---|
| 满闸 | 全量门禁 | QA gate | `just qa` 全量质量流水线 | ✅ 已替换 |
| 拍板 | 定案 | decision | 人类对方向 / 选型做最终决定 | ✅ 已替换 |
| 候补 | 候选（方向） | candidate | roadmaps 未启动方向 | ✅ 已替换 |
| 收口 | 闭合 | settled / closed | 对拍缺口补齐、事项完结 | ✅ 已替换 |
| 冻结（流程义） | 冷归档 | freeze-archive | change 归档进 `freezed_changes.7z.archived` | ✅ 已替换（命令 `archive freeze` 保留） |
| 冻结 / FROZEN（产品义） | 表定稿 | tool-table frozen | MCP 工具表定稿状态 | 保留（产品态词，spec ath27） |
| 票 | 提案工单 | proposal draft | SDD 草案工单 | 草稿域用语；活治理文档已无 |
| 台账 | 差异登记表 | ledger | PI_DELTAS 刻意差异记录 | 别名保留 |
| 分诊（死码） | 死代码分类处置 | triage | 真死删 / 激活 / 留三分类 | 已加 EN |
| 落地 | 实施 | deliver | 能力实际交付 | 半标准，低频保留 |

## B. 会话并发域

| 黑话 | 标准词 | EN | 定义 | 状态 |
|---|---|---|---|---|
| 单飞 | 单写者约束 | single-writer | runtime 显式绑定 session 后同时至多一个 ReAct worker 写入 | ✅ 已替换 |
| 插话 | 插话 | steer | 运行中注入用户输入改向本轮 | 双轨（首现带 EN） |
| 续跑 | 续跑 | follow-up | 本轮结束后排队执行追加指令 | 双轨 |
| 改道 | 改向 | in-turn redirect | steer / follow-up 轮内重定向统称 | ✅ 已替换 |
| 中止 | 中止 | abort | 取消当前运行 | 半标准 |

产品语义详情：[插话续跑与中止.md](./插话续跑与中止.md)。

## C. 架构词

| 黑话 | 标准词 | EN | 定义 | 状态 |
|---|---|---|---|---|
| 投影 | 投影 | projection | `AgentMessage` → LLM 视图映射（`project_for_llm`） | 首现已带 EN |
| 缝 / 内缝 / 整机缝 | 接缝 | seam | 可替换边界、端口接缝点 | ✅ 复合词已替换 |
| 产品信封 / 四象限 | 信封 | RPC envelope | client/server request/response 四象限封装协议 | 首现已带 EN |
| 三圈契约 | 三层契约 | — | 线协议 / 应用协议 / 可替换端口三层 | ✅ 已替换 |
| （LLM）叶 | LLM 叶层 | leaf | 最底层 LLM DTO（bridge 所有） | 首现已带 EN |
| 组合根 | 组合根 | composition root | 唯一同时装配 agent + infra 的入口（app/core 与各面入口） | 首现已带 EN |
| 盘面 | 磁盘格式 | on-disk format | 会话 JSONL 落盘格式 | ✅ 已替换 |
| 双写 ⚠️ | —（llman 域） | partitioned dual-write | llman specs 双通道写入流程；**勿**与「重复实现」混淆 | 保留（llman 域） |
| 上游 / vendor | 上游 SDK | upstream SDK | 官方厂商库来源 | 半标准保留 |

分层与契约细节：[`src/AGENTS.md`](../../src/AGENTS.md)。

## D. TUI 面词汇（标识符保留，中文别名在此备查）

固定词与退役词的 SSOT：[TUI信息面与chrome词汇.md](./TUI信息面与chrome词汇.md)。常用别名：

| 标识符 / 词 | 中文别名 | 含义 |
|---|---|---|
| chrome | 固定框架区 | 界面中非滚动内容的骨架区（状态条、页脚等） |
| dock | 输入坞 | 底部 editor 槽所在固定区 |
| plate | 命令面板组件 | `Plate` 组件（命令 / 设置面板底座） |
| bang | shell 直通前缀 | `!` / `!!` 用户直接跑终端命令 |
| cue | 预告短句 | busy 期右下角短提示（产品面已退役渲染；引擎 / harness 保留） |
| 尾随 / 顶插 | 底部跟随 / 顶部插入 | 滚动提示的两种插入位置策略 |

## E. 内部编号

| 形态 | 含义 | 查询 |
|---|---|---|
| `c####` | llman change id | [变更索引.md](./变更索引.md)（全量映射） |
| `ath##` / `atm##` / `ati##` / `ce##` 等 | 各 capability spec 需求 id | 所在 `llmanspec/specs/<capability>/<capability>.feature`（`@req:` 规则块） |
| `qg##` / `ptim##` / `w8` | 票内局部代号 | 对应草案文档 |

新写作规范：id 单独引用须带 ≤12 字语义尾（如 `c2040 tui-mouse-click-fold-triangle`）。
