# Dated nightly pin (application; not rolling `nightly`).
# Bump cadence: skill `xylitol-bump-toolchain`.
[toolchain]
channel = "nightly-2026-09-08"
profile = "minimal"
components = ["rustfmt", "clippy"]
