# HANDOFF — specs 约束产品级化（进行中）

> 临时记录（2026-08 规格维护会话）。勿升格规范。

## 背景与规则（用户拍板，已落 AGENTS.md）

specs 的 requirement 约束收敛到**产品级**（可观察行为/数据契约/验证结果），**不硬约束代码组织**（路径/模块名/类型名/行数/迁移清单），仅保留大的组织方向（分层依赖、端口 seam、crate 边界、组合根职责、跨面同源）。代码组织演进不要求改 spec；直接编辑 spec 免 change 生命周期（新增产品行为仍走标准 change）。

规则落点：`AGENTS.md` →「Specs 约束层级（产品级优先）」；`llmanspec/AGENTS.md` →「spec 约束层级」「spec 维护」。

## 已完成档位（各 commit 独立可验收）

| 档 | commit | 内容 | 验证 |
|---|---|---|---|
| A | `1c7fb4a` | agent-session 36→26 条（删 10 迁移/组织条款、8 条产品级改写、7 条主语清理）；连带 c12/ex1-3/s11/sp3/ip8/sr10 产品化；删 2 组织细节 BDD 场景（api-retained、snapshot-regenerated）+ bindings + 孤儿 steps | validate 5 spec 绿；BDD 270 passed |
| B | `365eee5` | layer-architecture 34→26（合并 la1+la5、la3+la9+la12、la16+la14；删 la10/la11/la18/ar02；改写 la4/la19）；agent-runtime ar1 删路径括号 | validate 2 spec 绿 |
| C | `74a3979` | ask 回调端口上移 `protocol::ports::ask`（AskUserGateway+4 DTO）；infra::tools::ask 只留 AskTool+default_tools_with_ask；删死代码 toolset_with_ask（无生产调用方）；tui4 补 agent::tools 禁词 | check 零警告；ask 单测 6 passed；BDD 270 passed |
| D | `5378aa5` | 删 app-tui-host ath9（QA 基线前置，一次性条款）+ 2 死场景（shared-bang-helper / bdd-features-present，无 binding） | validate app-tui-host 绿 |
| E | （待提交） | 删 agent-runtime ar14（禁 facade，重复且风险低）；ce7 删 facade 尾巴；保留 r69（禁 pi 短名=产品语义）、ce11（无 --rpc）、s4（数据兼容）、as43（trust 分层方向） | validate agent-runtime/cli-entry 绿 |

## 待办

- 全部档位完成（A/B/C/D/E）。收尾：可考虑合并 E 档遗留为"已删除物清单"（可选，非必须）

## 验证基线

- `llman sdd validate <cap>` 全绿；BDD `cargo test --test bdd` = 270 passed（基线 272，删 2 场景）
- commit 前 pre-commit 全过（fmt/clippy/卫生）；CI 常规 `just qa`
