/// Planner — placeholder (feature = "agent-planning").
/// Architecture/Editor/Validator separation. Expanded in c55.
pub(crate) struct Planner;

impl Planner {
    pub(crate) fn new() -> Self {
        Self
    }
}
