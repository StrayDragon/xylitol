/// Model registry — placeholder.
/// Handles model registration and locking. Expanded in c60.
pub(crate) struct ModelRegistry;

impl ModelRegistry {
    pub(crate) fn new() -> Self {
        Self
    }
}
