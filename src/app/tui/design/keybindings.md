# 快捷键

取消独立快捷键设计（不再做 Keybindings 百科槽 / 顶栏快捷键墙）。

组件自己的键写在对应模块 `draft.yaml` 的 `keys:`（树 Enter/Esc、`/model` 槽内 ←→、compaction Alt+E、队列 Alt+Enter/Alt+Up 等）。

入口：[`designing/tui/modules/`](../../../../designing/tui/modules/) · `just open-designing`。
