---
version: "alpha"
name: "footer"
description: "Xylitol Terminal component UX — see DESIGN.md index."
---

# Footer

## MUST

1. 恰好 **1 行** dim：`cwd (branch) · model · context%`；放不下截断右侧，**MUST NOT** 增高。
2. 快捷键提示：默认不列清单；需要时 `/help` 或极短 `?`。
3. **MUST NOT** 常驻多行 debug / 快捷键墙。

颜色：`muted`。
