//! Loader widget — animated spinner (c399 stage 2.4).
//!
//! Placeholder for stage 2.4. Reuses the SPINNER const from the old components
//! when wired in. For now this module exists so `widgets/mod.rs` resolves.

// TODO(c399 stage 2.4): spinner widget — frames + intervalMs, self-schedules
// renders via the host loop's tick (the sanctioned exception to "widgets don't
// schedule renders").
