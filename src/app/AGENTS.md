# src/app/ 应用面

本文件只给入口，不重复架构事实。分层 / 应用面状态 / seam SSOT：`src/AGENTS.md`；全局：根 `AGENTS.md`。

- 新增或改造应用面：`write-surface` skill
- 死代码分诊：`audit-dead-code` skill
- TUI 面：`src/app/tui/AGENTS.md` + `write-tui` skill
- TUI 五层验证：`test-tui-harness` skill
