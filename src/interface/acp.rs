/// ACP mode — placeholder (feature = "infra-acp").
/// IDE integration via agent-client-protocol. Expanded in c87.
pub(crate) struct Acp;

impl Acp {
    pub(crate) fn new() -> Self {
        Self
    }
}
