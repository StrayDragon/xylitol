/// DAP integration layer — placeholder (feature = "infra-dap").
/// Expanded in c85.
pub struct Dap;

impl Default for Dap {
    fn default() -> Self {
        Self::new()
    }
}

impl Dap {
    pub fn new() -> Self {
        Self
    }
}
